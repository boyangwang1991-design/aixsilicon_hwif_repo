CAPI=2:

name: aix:interface:hac_mem:0.1.0
description: AIXSILICON HAC-MEM system memory access interface contract and views

filesets:
  rtl:
    depend:
      - aix:interface:common:1.0.0
      - aix:interface:hac_if:0.1.0
    files:
      - rtl/aix_hac_mem_pkg.sv
      - rtl/aix_hac_mem_if.sv
    file_type: systemVerilogSource

  contract:
    files:
      - contract/hac_mem.interface.yaml: {copyto: metadata/hac_mem.interface.yaml}
    file_type: user

targets:
  default:
    filesets: [rtl]

  contract:
    filesets: [contract]

  lint:
    filesets: [rtl]
