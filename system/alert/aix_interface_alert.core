CAPI=2:

name: aix:interface:alert:0.1.0
description: AIXSILICON alert interface contract and SystemVerilog views

filesets:
  rtl:
    depend:
      - aix:interface:common:1.0.0
    files:
      - rtl/aix_alert_pkg.sv
      - rtl/aix_alert_if.sv
    file_type: systemVerilogSource

  contract:
    files:
      - contract/alert.interface.yaml: {copyto: metadata/alert.interface.yaml}
    file_type: user

targets:
  default:
    filesets: [rtl]

  contract:
    filesets: [contract]

  lint:
    filesets: [rtl]
