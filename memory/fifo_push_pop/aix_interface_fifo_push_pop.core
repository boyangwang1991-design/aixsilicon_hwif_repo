CAPI=2:

name: aix:interface:fifo_push_pop:1.0.0
description: AIXSILICON FIFO push/pop interface contract and views

filesets:
  rtl:
    depend:
      - aix:interface:common:1.0.0
    files:
      - rtl/aix_fifo_push_pop_pkg.sv
      - rtl/aix_fifo_push_pop_if.sv
    file_type: systemVerilogSource

  contract:
    files:
      - contract/fifo_push_pop.interface.yaml: {copyto: metadata/fifo_push_pop.interface.yaml}
    file_type: user

targets:
  default:
    filesets: [rtl]

  contract:
    filesets: [contract]

  lint:
    filesets: [rtl]
