CAPI=2:

name: aixsilicon:interface:security_violation:0.1.0
description: AIXSILICON security violation interface contract and SystemVerilog views

filesets:
  rtl:
    depend:
      - aixsilicon:interface:common:1.0.0
    files:
      - rtl/security_violation_pkg.sv
      - rtl/security_violation_if.sv
    file_type: systemVerilogSource

  contract:
    files:
      - contract/security_violation.interface.yaml: {copyto: metadata/security_violation.interface.yaml}
    file_type: user

targets:
  default:
    filesets: [rtl]

  contract:
    filesets: [contract]

  lint:
    filesets: [rtl]
