CAPI=2:

name: aix:interface:rom:0.1.0
description: AIXSILICON ROM interface contract and SystemVerilog views

filesets:
  rtl:
    depend:
      - aix:interface:common:1.0.0
    files:
      - rtl/aix_rom_pkg.sv
      - rtl/aix_rom_if.sv
    file_type: systemVerilogSource

  contract:
    files:
      - contract/rom.interface.yaml: {copyto: metadata/rom.interface.yaml}
    file_type: user

targets:
  default:
    filesets: [rtl]

  contract:
    filesets: [contract]

  lint:
    filesets: [rtl]
