CAPI=2:

name: aixsilicon:interface:reg_native:1.0.0
description: AIXSILICON native register interface contract and views

filesets:
  rtl:
    depend:
      - aixsilicon:interface:common:1.0.0
    files:
      - rtl/reg_native_pkg.sv
      - rtl/reg_native_if.sv
    file_type: systemVerilogSource

  contract:
    files:
      - contract/reg_native.interface.yaml: {copyto: metadata/reg_native.interface.yaml}
    file_type: user

targets:
  default:
    filesets: [rtl]

  contract:
    filesets: [contract]

  lint:
    filesets: [rtl]
