CAPI=2:

name: aixsilicon:interface:status_control:0.1.0
description: AIXSILICON status/control interface contract and SystemVerilog views

filesets:
  rtl:
    depend:
      - aixsilicon:interface:common:1.0.0
    files:
      - rtl/status_control_pkg.sv
      - rtl/status_control_if.sv
    file_type: systemVerilogSource

  contract:
    files:
      - contract/status_control.interface.yaml: {copyto: metadata/status_control.interface.yaml}
    file_type: user

targets:
  default:
    filesets: [rtl]

  contract:
    filesets: [contract]

  lint:
    filesets: [rtl]
