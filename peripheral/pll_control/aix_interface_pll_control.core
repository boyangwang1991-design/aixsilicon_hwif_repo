CAPI=2:

name: aix:interface:pll_control:0.1.0
description: AIXSILICON PLL control interface contract and SystemVerilog views

filesets:
  rtl:
    depend:
      - aix:interface:common:1.0.0
    files:
      - rtl/aix_pll_control_pkg.sv
      - rtl/aix_pll_control_if.sv
    file_type: systemVerilogSource

  contract:
    files:
      - contract/pll_control.interface.yaml: {copyto: metadata/pll_control.interface.yaml}
    file_type: user

targets:
  default:
    filesets: [rtl]

  contract:
    filesets: [contract]

  lint:
    filesets: [rtl]
