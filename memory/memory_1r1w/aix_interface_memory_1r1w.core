CAPI=2:

name: aix:interface:memory_1r1w:1.0.0
description: AIXSILICON separate read/write port memory interface contract and views

filesets:
  rtl:
    depend:
      - aix:interface:common:1.0.0
    files:
      - rtl/aix_memory_1r1w_pkg.sv
      - rtl/aix_memory_1r1w_if.sv
    file_type: systemVerilogSource

  contract:
    files:
      - contract/memory_1r1w.interface.yaml: {copyto: metadata/memory_1r1w.interface.yaml}
    file_type: user

targets:
  default:
    filesets: [rtl]

  contract:
    filesets: [contract]

  lint:
    filesets: [rtl]
