# Negative test: 非法 handshake 值应被拒绝（仅 ready_valid/req_ack/event/apb/ahb/none）
schema_version: "1.0"

interface:
  id: IFC-BAD-002
  name: aix_bad
  family: test
  semantic_version: 1.0.0
  owner: hw-platform
  lifecycle: draft

roles:
  - id: source
  - id: sink

channels:
  - id: ch
    handshake: bogus_handshake
    signals:
      - id: sig
        from: source
        to: sink
        width: "1"
        required: true

views:
  packed_struct: false
  sv_interface: true
  flattened: false
