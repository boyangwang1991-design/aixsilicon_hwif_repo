CAPI=2:

name: aixsilicon:interface:fifo_push_pop:1.0.0
description: AIXSILICON FIFO push/pop interface contract and views

filesets:
  rtl:
    depend:
      - aixsilicon:interface:common:1.0.0
    files:
      - rtl/fifo_push_pop_pkg.sv
      - rtl/fifo_push_pop_if.sv
    file_type: systemVerilogSource

  contract:
    files:
      - contract/fifo_push_pop.interface.yaml: {copyto: metadata/fifo_push_pop.interface.yaml}
    file_type: user

targets:
  default:
    filesets: [rtl]

  contract:
    filesets: [contract]

  lint:
    filesets: [rtl]
