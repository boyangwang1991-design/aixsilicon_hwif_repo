CAPI=2:

name: aixsilicon:interface:lbist_control:0.1.0
description: AIXSILICON LBIST control interface contract and SystemVerilog views

filesets:
  rtl:
    depend:
      - aixsilicon:interface:common:1.0.0
    files:
      - rtl/lbist_control_pkg.sv
      - rtl/lbist_control_if.sv
    file_type: systemVerilogSource

  contract:
    files:
      - contract/lbist_control.interface.yaml: {copyto: metadata/lbist_control.interface.yaml}
    file_type: user

targets:
  default:
    filesets: [rtl]

  contract:
    filesets: [contract]

  lint:
    filesets: [rtl]
