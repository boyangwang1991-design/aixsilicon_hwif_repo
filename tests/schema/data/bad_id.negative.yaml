# Negative test: 非法稳定 ID 应被拒绝（需匹配 ^IFC-[A-Z0-9_]+-[0-9]{3}$）
schema_version: "1.0"

interface:
  id: bad-id
  name: aix_bad
  family: test
  semantic_version: 1.0.0
  owner: hw-platform
  lifecycle: draft

roles:
  - id: source

channels:
  - id: ch
    handshake: none
    signals:
      - id: sig
        from: source
        to: source
        width: "1"
        required: true

views:
  packed_struct: false
  sv_interface: true
  flattened: false
