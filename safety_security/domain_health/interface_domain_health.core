CAPI=2:

name: aixsilicon:interface:domain_health:0.1.0
description: AIXSILICON domain health interface contract and SystemVerilog views

filesets:
  rtl:
    depend:
      - aixsilicon:interface:common:1.0.0
    files:
      - rtl/domain_health_pkg.sv
      - rtl/domain_health_if.sv
    file_type: systemVerilogSource

  contract:
    files:
      - contract/domain_health.interface.yaml: {copyto: metadata/domain_health.interface.yaml}
    file_type: user

targets:
  default:
    filesets: [rtl]

  contract:
    filesets: [contract]

  lint:
    filesets: [rtl]
