// SPDX-License-Identifier: Apache-2.0
// Generated by examples/crypto_interfaces/generate_smoke.py; no service model.
// Contract SHA256: 07a0097e2716eabc489b2596986872775e853285a3364dd6c7c3d44a90f0d1e7
`timescale 1ns/1ps
module crypto_secret_peer_32(aix_crypto_secret_if.service link,
  input logic [1276:0] response, output logic [1866:0] observed);
  assign link.req_ready = response[1276 +: 1];
  assign link.rsp_valid = response[1275 +: 1];
  assign link.rsp_owner_id = response[1243 +: 32];
  assign link.rsp_instance_id = response[1227 +: 16];
  assign link.rsp_epoch = response[1195 +: 32];
  assign link.rsp_task_id = response[1163 +: 32];
  assign link.rsp_request_seq = response[1099 +: 64];
  assign link.rsp_context_id = response[1083 +: 16];
  assign link.rsp_generation = response[1051 +: 32];
  assign link.rsp_request_id = response[987 +: 64];
  assign link.rsp_status = response[971 +: 16];
  assign link.rsp_object_ref = response[907 +: 64];
  assign link.rsp_object_epoch = response[875 +: 32];
  assign link.rsp_total_length = response[811 +: 64];
  assign link.rsp_offset = response[747 +: 64];
  assign link.rsp_fragment_length = response[715 +: 32];
  assign link.rsp_end = response[714 +: 1];
  assign link.rsp_object_state = response[706 +: 8];
  assign link.write_ready = response[705 +: 1];
  assign link.read_valid = response[704 +: 1];
  assign link.read_owner_id = response[672 +: 32];
  assign link.read_instance_id = response[656 +: 16];
  assign link.read_epoch = response[624 +: 32];
  assign link.read_task_id = response[592 +: 32];
  assign link.read_request_seq = response[528 +: 64];
  assign link.read_context_id = response[512 +: 16];
  assign link.read_generation = response[480 +: 32];
  assign link.read_request_id = response[416 +: 64];
  assign link.read_offset = response[352 +: 64];
  assign link.read_data = response[320 +: 32];
  assign link.read_keep = response[316 +: 4];
  assign link.read_last = response[315 +: 1];
  assign link.control_req_ready = response[314 +: 1];
  assign link.control_rsp_valid = response[313 +: 1];
  assign link.control_rsp_owner_id = response[281 +: 32];
  assign link.control_rsp_instance_id = response[265 +: 16];
  assign link.control_rsp_epoch = response[233 +: 32];
  assign link.control_rsp_task_id = response[201 +: 32];
  assign link.control_rsp_request_seq = response[137 +: 64];
  assign link.control_rsp_context_id = response[121 +: 16];
  assign link.control_rsp_generation = response[89 +: 32];
  assign link.control_rsp_request_id = response[25 +: 64];
  assign link.control_rsp_status = response[9 +: 16];
  assign link.control_rsp_object_state = response[1 +: 8];
  assign link.control_rsp_cleanup_done = response[0 +: 1];
  assign observed = {link.req_valid, link.req_owner_id, link.req_instance_id, link.req_epoch, link.req_task_id, link.req_request_seq, link.req_context_id, link.req_generation, link.req_service_version, link.req_request_id, link.req_policy_epoch, link.req_key_epoch, link.req_role, link.req_object_ref, link.req_offset, link.req_length, link.req_operation, link.req_mode, link.req_use, link.req_result_sink_ref, link.req_usage_amount, link.rsp_ready, link.write_valid, link.write_owner_id, link.write_instance_id, link.write_epoch, link.write_task_id, link.write_request_seq, link.write_context_id, link.write_generation, link.write_request_id, link.write_offset, link.write_data, link.write_keep, link.write_last, link.read_ready, link.control_req_valid, link.control_req_owner_id, link.control_req_instance_id, link.control_req_epoch, link.control_req_task_id, link.control_req_request_seq, link.control_req_context_id, link.control_req_generation, link.control_req_service_version, link.control_req_request_id, link.control_req_policy_epoch, link.control_req_key_epoch, link.control_req_role, link.control_req_object_ref, link.control_req_offset, link.control_req_length, link.control_req_operation, link.control_req_mode, link.control_req_use, link.control_req_result_sink_ref, link.control_req_usage_amount, link.control_rsp_ready};
  always @(posedge link.clk) if (link.reset_n !== 1'b0 && link.reset_n !== 1'b1) $fatal(1,"reset modport");
endmodule
module crypto_secret_case_32(input logic clk, reset_n, output logic done);
  import aix_crypto_secret_pkg::*;
  aix_crypto_secret_w32_req_t request;
  aix_crypto_secret_w32_rsp_t response;
  logic [1276:0] expected;
  logic [1866:0] observed;
  aix_crypto_secret_if #(.DATA_W(32)) link (.clk(clk), .reset_n(reset_n));
  crypto_secret_peer_32 peer(link, expected, observed);
  aix_crypto_secret_flat_wrapper #(.DATA_W(32)) bridge(.link(link),
    .req_valid_i(request.req_valid),
    .req_ready_o(response.req_ready),
    .req_owner_id_i(request.req_owner_id),
    .req_instance_id_i(request.req_instance_id),
    .req_epoch_i(request.req_epoch),
    .req_task_id_i(request.req_task_id),
    .req_request_seq_i(request.req_request_seq),
    .req_context_id_i(request.req_context_id),
    .req_generation_i(request.req_generation),
    .req_service_version_i(request.req_service_version),
    .req_request_id_i(request.req_request_id),
    .req_policy_epoch_i(request.req_policy_epoch),
    .req_key_epoch_i(request.req_key_epoch),
    .req_role_i(request.req_role),
    .req_object_ref_i(request.req_object_ref),
    .req_offset_i(request.req_offset),
    .req_length_i(request.req_length),
    .req_operation_i(request.req_operation),
    .req_mode_i(request.req_mode),
    .req_use_i(request.req_use),
    .req_result_sink_ref_i(request.req_result_sink_ref),
    .req_usage_amount_i(request.req_usage_amount),
    .rsp_valid_o(response.rsp_valid),
    .rsp_ready_i(request.rsp_ready),
    .rsp_owner_id_o(response.rsp_owner_id),
    .rsp_instance_id_o(response.rsp_instance_id),
    .rsp_epoch_o(response.rsp_epoch),
    .rsp_task_id_o(response.rsp_task_id),
    .rsp_request_seq_o(response.rsp_request_seq),
    .rsp_context_id_o(response.rsp_context_id),
    .rsp_generation_o(response.rsp_generation),
    .rsp_request_id_o(response.rsp_request_id),
    .rsp_status_o(response.rsp_status),
    .rsp_object_ref_o(response.rsp_object_ref),
    .rsp_object_epoch_o(response.rsp_object_epoch),
    .rsp_total_length_o(response.rsp_total_length),
    .rsp_offset_o(response.rsp_offset),
    .rsp_fragment_length_o(response.rsp_fragment_length),
    .rsp_end_o(response.rsp_end),
    .rsp_object_state_o(response.rsp_object_state),
    .write_valid_i(request.write_valid),
    .write_ready_o(response.write_ready),
    .write_owner_id_i(request.write_owner_id),
    .write_instance_id_i(request.write_instance_id),
    .write_epoch_i(request.write_epoch),
    .write_task_id_i(request.write_task_id),
    .write_request_seq_i(request.write_request_seq),
    .write_context_id_i(request.write_context_id),
    .write_generation_i(request.write_generation),
    .write_request_id_i(request.write_request_id),
    .write_offset_i(request.write_offset),
    .write_data_i(request.write_data),
    .write_keep_i(request.write_keep),
    .write_last_i(request.write_last),
    .read_valid_o(response.read_valid),
    .read_ready_i(request.read_ready),
    .read_owner_id_o(response.read_owner_id),
    .read_instance_id_o(response.read_instance_id),
    .read_epoch_o(response.read_epoch),
    .read_task_id_o(response.read_task_id),
    .read_request_seq_o(response.read_request_seq),
    .read_context_id_o(response.read_context_id),
    .read_generation_o(response.read_generation),
    .read_request_id_o(response.read_request_id),
    .read_offset_o(response.read_offset),
    .read_data_o(response.read_data),
    .read_keep_o(response.read_keep),
    .read_last_o(response.read_last),
    .control_req_valid_i(request.control_req_valid),
    .control_req_ready_o(response.control_req_ready),
    .control_req_owner_id_i(request.control_req_owner_id),
    .control_req_instance_id_i(request.control_req_instance_id),
    .control_req_epoch_i(request.control_req_epoch),
    .control_req_task_id_i(request.control_req_task_id),
    .control_req_request_seq_i(request.control_req_request_seq),
    .control_req_context_id_i(request.control_req_context_id),
    .control_req_generation_i(request.control_req_generation),
    .control_req_service_version_i(request.control_req_service_version),
    .control_req_request_id_i(request.control_req_request_id),
    .control_req_policy_epoch_i(request.control_req_policy_epoch),
    .control_req_key_epoch_i(request.control_req_key_epoch),
    .control_req_role_i(request.control_req_role),
    .control_req_object_ref_i(request.control_req_object_ref),
    .control_req_offset_i(request.control_req_offset),
    .control_req_length_i(request.control_req_length),
    .control_req_operation_i(request.control_req_operation),
    .control_req_mode_i(request.control_req_mode),
    .control_req_use_i(request.control_req_use),
    .control_req_result_sink_ref_i(request.control_req_result_sink_ref),
    .control_req_usage_amount_i(request.control_req_usage_amount),
    .control_rsp_valid_o(response.control_rsp_valid),
    .control_rsp_ready_i(request.control_rsp_ready),
    .control_rsp_owner_id_o(response.control_rsp_owner_id),
    .control_rsp_instance_id_o(response.control_rsp_instance_id),
    .control_rsp_epoch_o(response.control_rsp_epoch),
    .control_rsp_task_id_o(response.control_rsp_task_id),
    .control_rsp_request_seq_o(response.control_rsp_request_seq),
    .control_rsp_context_id_o(response.control_rsp_context_id),
    .control_rsp_generation_o(response.control_rsp_generation),
    .control_rsp_request_id_o(response.control_rsp_request_id),
    .control_rsp_status_o(response.control_rsp_status),
    .control_rsp_object_state_o(response.control_rsp_object_state),
    .control_rsp_cleanup_done_o(response.control_rsp_cleanup_done)
  );
  function automatic logic [1866:0] pattern(input int seed);
    int unsigned state; state=32'h9e3779b9 ^ (32'h85ebca6b * seed);
    for (int i=0;i<1867;i++) begin
      state=1664525*state+1013904223; pattern[i]=state[31];
    end
  endfunction
  task automatic compare_paths;
    if (observed !== request) $fatal(1,"crypto_secret W32 forward wiring");
    if (response !== expected) $fatal(1,"crypto_secret W32 reverse wiring");
  endtask
  initial begin
    done=0; request='0; expected='0;
    if ($bits(request)!=1867 || $bits(response)!=1277) $fatal(1,"aggregate width");
    if ($bits(link.req_valid)!=1) $fatal(1,"req_valid width");
    if ($bits(link.req_ready)!=1) $fatal(1,"req_ready width");
    if ($bits(link.req_owner_id)!=32) $fatal(1,"req_owner_id width");
    if ($bits(link.req_instance_id)!=16) $fatal(1,"req_instance_id width");
    if ($bits(link.req_epoch)!=32) $fatal(1,"req_epoch width");
    if ($bits(link.req_task_id)!=32) $fatal(1,"req_task_id width");
    if ($bits(link.req_request_seq)!=64) $fatal(1,"req_request_seq width");
    if ($bits(link.req_context_id)!=16) $fatal(1,"req_context_id width");
    if ($bits(link.req_generation)!=32) $fatal(1,"req_generation width");
    if ($bits(link.req_service_version)!=16) $fatal(1,"req_service_version width");
    if ($bits(link.req_request_id)!=64) $fatal(1,"req_request_id width");
    if ($bits(link.req_policy_epoch)!=32) $fatal(1,"req_policy_epoch width");
    if ($bits(link.req_key_epoch)!=32) $fatal(1,"req_key_epoch width");
    if ($bits(link.req_role)!=8) $fatal(1,"req_role width");
    if ($bits(link.req_object_ref)!=64) $fatal(1,"req_object_ref width");
    if ($bits(link.req_offset)!=64) $fatal(1,"req_offset width");
    if ($bits(link.req_length)!=64) $fatal(1,"req_length width");
    if ($bits(link.req_operation)!=16) $fatal(1,"req_operation width");
    if ($bits(link.req_mode)!=8) $fatal(1,"req_mode width");
    if ($bits(link.req_use)!=16) $fatal(1,"req_use width");
    if ($bits(link.req_result_sink_ref)!=64) $fatal(1,"req_result_sink_ref width");
    if ($bits(link.req_usage_amount)!=64) $fatal(1,"req_usage_amount width");
    if ($bits(link.rsp_valid)!=1) $fatal(1,"rsp_valid width");
    if ($bits(link.rsp_ready)!=1) $fatal(1,"rsp_ready width");
    if ($bits(link.rsp_owner_id)!=32) $fatal(1,"rsp_owner_id width");
    if ($bits(link.rsp_instance_id)!=16) $fatal(1,"rsp_instance_id width");
    if ($bits(link.rsp_epoch)!=32) $fatal(1,"rsp_epoch width");
    if ($bits(link.rsp_task_id)!=32) $fatal(1,"rsp_task_id width");
    if ($bits(link.rsp_request_seq)!=64) $fatal(1,"rsp_request_seq width");
    if ($bits(link.rsp_context_id)!=16) $fatal(1,"rsp_context_id width");
    if ($bits(link.rsp_generation)!=32) $fatal(1,"rsp_generation width");
    if ($bits(link.rsp_request_id)!=64) $fatal(1,"rsp_request_id width");
    if ($bits(link.rsp_status)!=16) $fatal(1,"rsp_status width");
    if ($bits(link.rsp_object_ref)!=64) $fatal(1,"rsp_object_ref width");
    if ($bits(link.rsp_object_epoch)!=32) $fatal(1,"rsp_object_epoch width");
    if ($bits(link.rsp_total_length)!=64) $fatal(1,"rsp_total_length width");
    if ($bits(link.rsp_offset)!=64) $fatal(1,"rsp_offset width");
    if ($bits(link.rsp_fragment_length)!=32) $fatal(1,"rsp_fragment_length width");
    if ($bits(link.rsp_end)!=1) $fatal(1,"rsp_end width");
    if ($bits(link.rsp_object_state)!=8) $fatal(1,"rsp_object_state width");
    if ($bits(link.write_valid)!=1) $fatal(1,"write_valid width");
    if ($bits(link.write_ready)!=1) $fatal(1,"write_ready width");
    if ($bits(link.write_owner_id)!=32) $fatal(1,"write_owner_id width");
    if ($bits(link.write_instance_id)!=16) $fatal(1,"write_instance_id width");
    if ($bits(link.write_epoch)!=32) $fatal(1,"write_epoch width");
    if ($bits(link.write_task_id)!=32) $fatal(1,"write_task_id width");
    if ($bits(link.write_request_seq)!=64) $fatal(1,"write_request_seq width");
    if ($bits(link.write_context_id)!=16) $fatal(1,"write_context_id width");
    if ($bits(link.write_generation)!=32) $fatal(1,"write_generation width");
    if ($bits(link.write_request_id)!=64) $fatal(1,"write_request_id width");
    if ($bits(link.write_offset)!=64) $fatal(1,"write_offset width");
    if ($bits(link.write_data)!=32) $fatal(1,"write_data width");
    if ($bits(link.write_keep)!=4) $fatal(1,"write_keep width");
    if ($bits(link.write_last)!=1) $fatal(1,"write_last width");
    if ($bits(link.read_valid)!=1) $fatal(1,"read_valid width");
    if ($bits(link.read_ready)!=1) $fatal(1,"read_ready width");
    if ($bits(link.read_owner_id)!=32) $fatal(1,"read_owner_id width");
    if ($bits(link.read_instance_id)!=16) $fatal(1,"read_instance_id width");
    if ($bits(link.read_epoch)!=32) $fatal(1,"read_epoch width");
    if ($bits(link.read_task_id)!=32) $fatal(1,"read_task_id width");
    if ($bits(link.read_request_seq)!=64) $fatal(1,"read_request_seq width");
    if ($bits(link.read_context_id)!=16) $fatal(1,"read_context_id width");
    if ($bits(link.read_generation)!=32) $fatal(1,"read_generation width");
    if ($bits(link.read_request_id)!=64) $fatal(1,"read_request_id width");
    if ($bits(link.read_offset)!=64) $fatal(1,"read_offset width");
    if ($bits(link.read_data)!=32) $fatal(1,"read_data width");
    if ($bits(link.read_keep)!=4) $fatal(1,"read_keep width");
    if ($bits(link.read_last)!=1) $fatal(1,"read_last width");
    if ($bits(link.control_req_valid)!=1) $fatal(1,"control_req_valid width");
    if ($bits(link.control_req_ready)!=1) $fatal(1,"control_req_ready width");
    if ($bits(link.control_req_owner_id)!=32) $fatal(1,"control_req_owner_id width");
    if ($bits(link.control_req_instance_id)!=16) $fatal(1,"control_req_instance_id width");
    if ($bits(link.control_req_epoch)!=32) $fatal(1,"control_req_epoch width");
    if ($bits(link.control_req_task_id)!=32) $fatal(1,"control_req_task_id width");
    if ($bits(link.control_req_request_seq)!=64) $fatal(1,"control_req_request_seq width");
    if ($bits(link.control_req_context_id)!=16) $fatal(1,"control_req_context_id width");
    if ($bits(link.control_req_generation)!=32) $fatal(1,"control_req_generation width");
    if ($bits(link.control_req_service_version)!=16) $fatal(1,"control_req_service_version width");
    if ($bits(link.control_req_request_id)!=64) $fatal(1,"control_req_request_id width");
    if ($bits(link.control_req_policy_epoch)!=32) $fatal(1,"control_req_policy_epoch width");
    if ($bits(link.control_req_key_epoch)!=32) $fatal(1,"control_req_key_epoch width");
    if ($bits(link.control_req_role)!=8) $fatal(1,"control_req_role width");
    if ($bits(link.control_req_object_ref)!=64) $fatal(1,"control_req_object_ref width");
    if ($bits(link.control_req_offset)!=64) $fatal(1,"control_req_offset width");
    if ($bits(link.control_req_length)!=64) $fatal(1,"control_req_length width");
    if ($bits(link.control_req_operation)!=16) $fatal(1,"control_req_operation width");
    if ($bits(link.control_req_mode)!=8) $fatal(1,"control_req_mode width");
    if ($bits(link.control_req_use)!=16) $fatal(1,"control_req_use width");
    if ($bits(link.control_req_result_sink_ref)!=64) $fatal(1,"control_req_result_sink_ref width");
    if ($bits(link.control_req_usage_amount)!=64) $fatal(1,"control_req_usage_amount width");
    if ($bits(link.control_rsp_valid)!=1) $fatal(1,"control_rsp_valid width");
    if ($bits(link.control_rsp_ready)!=1) $fatal(1,"control_rsp_ready width");
    if ($bits(link.control_rsp_owner_id)!=32) $fatal(1,"control_rsp_owner_id width");
    if ($bits(link.control_rsp_instance_id)!=16) $fatal(1,"control_rsp_instance_id width");
    if ($bits(link.control_rsp_epoch)!=32) $fatal(1,"control_rsp_epoch width");
    if ($bits(link.control_rsp_task_id)!=32) $fatal(1,"control_rsp_task_id width");
    if ($bits(link.control_rsp_request_seq)!=64) $fatal(1,"control_rsp_request_seq width");
    if ($bits(link.control_rsp_context_id)!=16) $fatal(1,"control_rsp_context_id width");
    if ($bits(link.control_rsp_generation)!=32) $fatal(1,"control_rsp_generation width");
    if ($bits(link.control_rsp_request_id)!=64) $fatal(1,"control_rsp_request_id width");
    if ($bits(link.control_rsp_status)!=16) $fatal(1,"control_rsp_status width");
    if ($bits(link.control_rsp_object_state)!=8) $fatal(1,"control_rsp_object_state width");
    if ($bits(link.control_rsp_cleanup_done)!=1) $fatal(1,"control_rsp_cleanup_done width");
    if ($bits(aix_crypto_secret_w32_req_payload_t)!=736) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w32_rsp_payload_t)!=569) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w32_write_payload_t)!=389) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w32_read_payload_t)!=389) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w32_control_req_payload_t)!=736) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w32_control_rsp_payload_t)!=313) $fatal(1,"payload width");
    if (ROLE_KEY_LOAD!=1) $fatal(1,"ROLE_KEY_LOAD encoding");
    if (ROLE_SECRET_MESSAGE_READ!=2) $fatal(1,"ROLE_SECRET_MESSAGE_READ encoding");
    if (ROLE_RESULT_PREPARE!=3) $fatal(1,"ROLE_RESULT_PREPARE encoding");
    if (ROLE_RESULT_WRITE!=4) $fatal(1,"ROLE_RESULT_WRITE encoding");
    if (ROLE_RESULT_COMMIT!=5) $fatal(1,"ROLE_RESULT_COMMIT encoding");
    if (ROLE_RESULT_ABORT!=6) $fatal(1,"ROLE_RESULT_ABORT encoding");
    if (ROLE_REVOKE!=7) $fatal(1,"ROLE_REVOKE encoding");
    if (ROLE_USAGE_RESERVE!=8) $fatal(1,"ROLE_USAGE_RESERVE encoding");
    if (ROLE_QUERY!=9) $fatal(1,"ROLE_QUERY encoding");
    if (ROLE_NONCE_RESERVE!=10) $fatal(1,"ROLE_NONCE_RESERVE encoding");
    if (USE_MAC!=1) $fatal(1,"USE_MAC encoding");
    if (USE_ENCRYPT!=2) $fatal(1,"USE_ENCRYPT encoding");
    if (USE_DECRYPT!=3) $fatal(1,"USE_DECRYPT encoding");
    if (USE_DERIVE!=4) $fatal(1,"USE_DERIVE encoding");
    if (USE_WRAP!=5) $fatal(1,"USE_WRAP encoding");
    if (USE_UNWRAP!=6) $fatal(1,"USE_UNWRAP encoding");
    if (USE_HP!=7) $fatal(1,"USE_HP encoding");
    if (OBJECT_STATE_NONE!=0) $fatal(1,"OBJECT_STATE_NONE encoding");
    if (OBJECT_STATE_PREPARED!=1) $fatal(1,"OBJECT_STATE_PREPARED encoding");
    if (OBJECT_STATE_WRITTEN!=2) $fatal(1,"OBJECT_STATE_WRITTEN encoding");
    if (OBJECT_STATE_ACTIVE!=3) $fatal(1,"OBJECT_STATE_ACTIVE encoding");
    if (OBJECT_STATE_ABORTED!=4) $fatal(1,"OBJECT_STATE_ABORTED encoding");
    if (OBJECT_STATE_UNKNOWN!=5) $fatal(1,"OBJECT_STATE_UNKNOWN encoding");
    wait(reset_n); #1; compare_paths();
    request='1; expected='1; #1; compare_paths();
    for (int sample=1;sample<=128;sample++) begin
      request=1867'(pattern(sample)); expected=1277'(pattern(sample+271));
      #1; compare_paths();
    end
    for (int bitno=0;bitno<1867;bitno++) begin
      request='0; request[bitno]=1'b1; #1; compare_paths(); end
    for (int bitno=0;bitno<1277;bitno++) begin
      expected='0; expected[bitno]=1'b1; #1; compare_paths(); end
    $display("PASS crypto_secret DATA_W=32 fields=103"); done=1;
  end
endmodule
module crypto_secret_peer_64(aix_crypto_secret_if.service link,
  input logic [1312:0] response, output logic [1902:0] observed);
  assign link.req_ready = response[1312 +: 1];
  assign link.rsp_valid = response[1311 +: 1];
  assign link.rsp_owner_id = response[1279 +: 32];
  assign link.rsp_instance_id = response[1263 +: 16];
  assign link.rsp_epoch = response[1231 +: 32];
  assign link.rsp_task_id = response[1199 +: 32];
  assign link.rsp_request_seq = response[1135 +: 64];
  assign link.rsp_context_id = response[1119 +: 16];
  assign link.rsp_generation = response[1087 +: 32];
  assign link.rsp_request_id = response[1023 +: 64];
  assign link.rsp_status = response[1007 +: 16];
  assign link.rsp_object_ref = response[943 +: 64];
  assign link.rsp_object_epoch = response[911 +: 32];
  assign link.rsp_total_length = response[847 +: 64];
  assign link.rsp_offset = response[783 +: 64];
  assign link.rsp_fragment_length = response[751 +: 32];
  assign link.rsp_end = response[750 +: 1];
  assign link.rsp_object_state = response[742 +: 8];
  assign link.write_ready = response[741 +: 1];
  assign link.read_valid = response[740 +: 1];
  assign link.read_owner_id = response[708 +: 32];
  assign link.read_instance_id = response[692 +: 16];
  assign link.read_epoch = response[660 +: 32];
  assign link.read_task_id = response[628 +: 32];
  assign link.read_request_seq = response[564 +: 64];
  assign link.read_context_id = response[548 +: 16];
  assign link.read_generation = response[516 +: 32];
  assign link.read_request_id = response[452 +: 64];
  assign link.read_offset = response[388 +: 64];
  assign link.read_data = response[324 +: 64];
  assign link.read_keep = response[316 +: 8];
  assign link.read_last = response[315 +: 1];
  assign link.control_req_ready = response[314 +: 1];
  assign link.control_rsp_valid = response[313 +: 1];
  assign link.control_rsp_owner_id = response[281 +: 32];
  assign link.control_rsp_instance_id = response[265 +: 16];
  assign link.control_rsp_epoch = response[233 +: 32];
  assign link.control_rsp_task_id = response[201 +: 32];
  assign link.control_rsp_request_seq = response[137 +: 64];
  assign link.control_rsp_context_id = response[121 +: 16];
  assign link.control_rsp_generation = response[89 +: 32];
  assign link.control_rsp_request_id = response[25 +: 64];
  assign link.control_rsp_status = response[9 +: 16];
  assign link.control_rsp_object_state = response[1 +: 8];
  assign link.control_rsp_cleanup_done = response[0 +: 1];
  assign observed = {link.req_valid, link.req_owner_id, link.req_instance_id, link.req_epoch, link.req_task_id, link.req_request_seq, link.req_context_id, link.req_generation, link.req_service_version, link.req_request_id, link.req_policy_epoch, link.req_key_epoch, link.req_role, link.req_object_ref, link.req_offset, link.req_length, link.req_operation, link.req_mode, link.req_use, link.req_result_sink_ref, link.req_usage_amount, link.rsp_ready, link.write_valid, link.write_owner_id, link.write_instance_id, link.write_epoch, link.write_task_id, link.write_request_seq, link.write_context_id, link.write_generation, link.write_request_id, link.write_offset, link.write_data, link.write_keep, link.write_last, link.read_ready, link.control_req_valid, link.control_req_owner_id, link.control_req_instance_id, link.control_req_epoch, link.control_req_task_id, link.control_req_request_seq, link.control_req_context_id, link.control_req_generation, link.control_req_service_version, link.control_req_request_id, link.control_req_policy_epoch, link.control_req_key_epoch, link.control_req_role, link.control_req_object_ref, link.control_req_offset, link.control_req_length, link.control_req_operation, link.control_req_mode, link.control_req_use, link.control_req_result_sink_ref, link.control_req_usage_amount, link.control_rsp_ready};
  always @(posedge link.clk) if (link.reset_n !== 1'b0 && link.reset_n !== 1'b1) $fatal(1,"reset modport");
endmodule
module crypto_secret_case_64(input logic clk, reset_n, output logic done);
  import aix_crypto_secret_pkg::*;
  aix_crypto_secret_w64_req_t request;
  aix_crypto_secret_w64_rsp_t response;
  logic [1312:0] expected;
  logic [1902:0] observed;
  aix_crypto_secret_if #(.DATA_W(64)) link (.clk(clk), .reset_n(reset_n));
  crypto_secret_peer_64 peer(link, expected, observed);
  aix_crypto_secret_flat_wrapper #(.DATA_W(64)) bridge(.link(link),
    .req_valid_i(request.req_valid),
    .req_ready_o(response.req_ready),
    .req_owner_id_i(request.req_owner_id),
    .req_instance_id_i(request.req_instance_id),
    .req_epoch_i(request.req_epoch),
    .req_task_id_i(request.req_task_id),
    .req_request_seq_i(request.req_request_seq),
    .req_context_id_i(request.req_context_id),
    .req_generation_i(request.req_generation),
    .req_service_version_i(request.req_service_version),
    .req_request_id_i(request.req_request_id),
    .req_policy_epoch_i(request.req_policy_epoch),
    .req_key_epoch_i(request.req_key_epoch),
    .req_role_i(request.req_role),
    .req_object_ref_i(request.req_object_ref),
    .req_offset_i(request.req_offset),
    .req_length_i(request.req_length),
    .req_operation_i(request.req_operation),
    .req_mode_i(request.req_mode),
    .req_use_i(request.req_use),
    .req_result_sink_ref_i(request.req_result_sink_ref),
    .req_usage_amount_i(request.req_usage_amount),
    .rsp_valid_o(response.rsp_valid),
    .rsp_ready_i(request.rsp_ready),
    .rsp_owner_id_o(response.rsp_owner_id),
    .rsp_instance_id_o(response.rsp_instance_id),
    .rsp_epoch_o(response.rsp_epoch),
    .rsp_task_id_o(response.rsp_task_id),
    .rsp_request_seq_o(response.rsp_request_seq),
    .rsp_context_id_o(response.rsp_context_id),
    .rsp_generation_o(response.rsp_generation),
    .rsp_request_id_o(response.rsp_request_id),
    .rsp_status_o(response.rsp_status),
    .rsp_object_ref_o(response.rsp_object_ref),
    .rsp_object_epoch_o(response.rsp_object_epoch),
    .rsp_total_length_o(response.rsp_total_length),
    .rsp_offset_o(response.rsp_offset),
    .rsp_fragment_length_o(response.rsp_fragment_length),
    .rsp_end_o(response.rsp_end),
    .rsp_object_state_o(response.rsp_object_state),
    .write_valid_i(request.write_valid),
    .write_ready_o(response.write_ready),
    .write_owner_id_i(request.write_owner_id),
    .write_instance_id_i(request.write_instance_id),
    .write_epoch_i(request.write_epoch),
    .write_task_id_i(request.write_task_id),
    .write_request_seq_i(request.write_request_seq),
    .write_context_id_i(request.write_context_id),
    .write_generation_i(request.write_generation),
    .write_request_id_i(request.write_request_id),
    .write_offset_i(request.write_offset),
    .write_data_i(request.write_data),
    .write_keep_i(request.write_keep),
    .write_last_i(request.write_last),
    .read_valid_o(response.read_valid),
    .read_ready_i(request.read_ready),
    .read_owner_id_o(response.read_owner_id),
    .read_instance_id_o(response.read_instance_id),
    .read_epoch_o(response.read_epoch),
    .read_task_id_o(response.read_task_id),
    .read_request_seq_o(response.read_request_seq),
    .read_context_id_o(response.read_context_id),
    .read_generation_o(response.read_generation),
    .read_request_id_o(response.read_request_id),
    .read_offset_o(response.read_offset),
    .read_data_o(response.read_data),
    .read_keep_o(response.read_keep),
    .read_last_o(response.read_last),
    .control_req_valid_i(request.control_req_valid),
    .control_req_ready_o(response.control_req_ready),
    .control_req_owner_id_i(request.control_req_owner_id),
    .control_req_instance_id_i(request.control_req_instance_id),
    .control_req_epoch_i(request.control_req_epoch),
    .control_req_task_id_i(request.control_req_task_id),
    .control_req_request_seq_i(request.control_req_request_seq),
    .control_req_context_id_i(request.control_req_context_id),
    .control_req_generation_i(request.control_req_generation),
    .control_req_service_version_i(request.control_req_service_version),
    .control_req_request_id_i(request.control_req_request_id),
    .control_req_policy_epoch_i(request.control_req_policy_epoch),
    .control_req_key_epoch_i(request.control_req_key_epoch),
    .control_req_role_i(request.control_req_role),
    .control_req_object_ref_i(request.control_req_object_ref),
    .control_req_offset_i(request.control_req_offset),
    .control_req_length_i(request.control_req_length),
    .control_req_operation_i(request.control_req_operation),
    .control_req_mode_i(request.control_req_mode),
    .control_req_use_i(request.control_req_use),
    .control_req_result_sink_ref_i(request.control_req_result_sink_ref),
    .control_req_usage_amount_i(request.control_req_usage_amount),
    .control_rsp_valid_o(response.control_rsp_valid),
    .control_rsp_ready_i(request.control_rsp_ready),
    .control_rsp_owner_id_o(response.control_rsp_owner_id),
    .control_rsp_instance_id_o(response.control_rsp_instance_id),
    .control_rsp_epoch_o(response.control_rsp_epoch),
    .control_rsp_task_id_o(response.control_rsp_task_id),
    .control_rsp_request_seq_o(response.control_rsp_request_seq),
    .control_rsp_context_id_o(response.control_rsp_context_id),
    .control_rsp_generation_o(response.control_rsp_generation),
    .control_rsp_request_id_o(response.control_rsp_request_id),
    .control_rsp_status_o(response.control_rsp_status),
    .control_rsp_object_state_o(response.control_rsp_object_state),
    .control_rsp_cleanup_done_o(response.control_rsp_cleanup_done)
  );
  function automatic logic [1902:0] pattern(input int seed);
    int unsigned state; state=32'h9e3779b9 ^ (32'h85ebca6b * seed);
    for (int i=0;i<1903;i++) begin
      state=1664525*state+1013904223; pattern[i]=state[31];
    end
  endfunction
  task automatic compare_paths;
    if (observed !== request) $fatal(1,"crypto_secret W64 forward wiring");
    if (response !== expected) $fatal(1,"crypto_secret W64 reverse wiring");
  endtask
  initial begin
    done=0; request='0; expected='0;
    if ($bits(request)!=1903 || $bits(response)!=1313) $fatal(1,"aggregate width");
    if ($bits(link.req_valid)!=1) $fatal(1,"req_valid width");
    if ($bits(link.req_ready)!=1) $fatal(1,"req_ready width");
    if ($bits(link.req_owner_id)!=32) $fatal(1,"req_owner_id width");
    if ($bits(link.req_instance_id)!=16) $fatal(1,"req_instance_id width");
    if ($bits(link.req_epoch)!=32) $fatal(1,"req_epoch width");
    if ($bits(link.req_task_id)!=32) $fatal(1,"req_task_id width");
    if ($bits(link.req_request_seq)!=64) $fatal(1,"req_request_seq width");
    if ($bits(link.req_context_id)!=16) $fatal(1,"req_context_id width");
    if ($bits(link.req_generation)!=32) $fatal(1,"req_generation width");
    if ($bits(link.req_service_version)!=16) $fatal(1,"req_service_version width");
    if ($bits(link.req_request_id)!=64) $fatal(1,"req_request_id width");
    if ($bits(link.req_policy_epoch)!=32) $fatal(1,"req_policy_epoch width");
    if ($bits(link.req_key_epoch)!=32) $fatal(1,"req_key_epoch width");
    if ($bits(link.req_role)!=8) $fatal(1,"req_role width");
    if ($bits(link.req_object_ref)!=64) $fatal(1,"req_object_ref width");
    if ($bits(link.req_offset)!=64) $fatal(1,"req_offset width");
    if ($bits(link.req_length)!=64) $fatal(1,"req_length width");
    if ($bits(link.req_operation)!=16) $fatal(1,"req_operation width");
    if ($bits(link.req_mode)!=8) $fatal(1,"req_mode width");
    if ($bits(link.req_use)!=16) $fatal(1,"req_use width");
    if ($bits(link.req_result_sink_ref)!=64) $fatal(1,"req_result_sink_ref width");
    if ($bits(link.req_usage_amount)!=64) $fatal(1,"req_usage_amount width");
    if ($bits(link.rsp_valid)!=1) $fatal(1,"rsp_valid width");
    if ($bits(link.rsp_ready)!=1) $fatal(1,"rsp_ready width");
    if ($bits(link.rsp_owner_id)!=32) $fatal(1,"rsp_owner_id width");
    if ($bits(link.rsp_instance_id)!=16) $fatal(1,"rsp_instance_id width");
    if ($bits(link.rsp_epoch)!=32) $fatal(1,"rsp_epoch width");
    if ($bits(link.rsp_task_id)!=32) $fatal(1,"rsp_task_id width");
    if ($bits(link.rsp_request_seq)!=64) $fatal(1,"rsp_request_seq width");
    if ($bits(link.rsp_context_id)!=16) $fatal(1,"rsp_context_id width");
    if ($bits(link.rsp_generation)!=32) $fatal(1,"rsp_generation width");
    if ($bits(link.rsp_request_id)!=64) $fatal(1,"rsp_request_id width");
    if ($bits(link.rsp_status)!=16) $fatal(1,"rsp_status width");
    if ($bits(link.rsp_object_ref)!=64) $fatal(1,"rsp_object_ref width");
    if ($bits(link.rsp_object_epoch)!=32) $fatal(1,"rsp_object_epoch width");
    if ($bits(link.rsp_total_length)!=64) $fatal(1,"rsp_total_length width");
    if ($bits(link.rsp_offset)!=64) $fatal(1,"rsp_offset width");
    if ($bits(link.rsp_fragment_length)!=32) $fatal(1,"rsp_fragment_length width");
    if ($bits(link.rsp_end)!=1) $fatal(1,"rsp_end width");
    if ($bits(link.rsp_object_state)!=8) $fatal(1,"rsp_object_state width");
    if ($bits(link.write_valid)!=1) $fatal(1,"write_valid width");
    if ($bits(link.write_ready)!=1) $fatal(1,"write_ready width");
    if ($bits(link.write_owner_id)!=32) $fatal(1,"write_owner_id width");
    if ($bits(link.write_instance_id)!=16) $fatal(1,"write_instance_id width");
    if ($bits(link.write_epoch)!=32) $fatal(1,"write_epoch width");
    if ($bits(link.write_task_id)!=32) $fatal(1,"write_task_id width");
    if ($bits(link.write_request_seq)!=64) $fatal(1,"write_request_seq width");
    if ($bits(link.write_context_id)!=16) $fatal(1,"write_context_id width");
    if ($bits(link.write_generation)!=32) $fatal(1,"write_generation width");
    if ($bits(link.write_request_id)!=64) $fatal(1,"write_request_id width");
    if ($bits(link.write_offset)!=64) $fatal(1,"write_offset width");
    if ($bits(link.write_data)!=64) $fatal(1,"write_data width");
    if ($bits(link.write_keep)!=8) $fatal(1,"write_keep width");
    if ($bits(link.write_last)!=1) $fatal(1,"write_last width");
    if ($bits(link.read_valid)!=1) $fatal(1,"read_valid width");
    if ($bits(link.read_ready)!=1) $fatal(1,"read_ready width");
    if ($bits(link.read_owner_id)!=32) $fatal(1,"read_owner_id width");
    if ($bits(link.read_instance_id)!=16) $fatal(1,"read_instance_id width");
    if ($bits(link.read_epoch)!=32) $fatal(1,"read_epoch width");
    if ($bits(link.read_task_id)!=32) $fatal(1,"read_task_id width");
    if ($bits(link.read_request_seq)!=64) $fatal(1,"read_request_seq width");
    if ($bits(link.read_context_id)!=16) $fatal(1,"read_context_id width");
    if ($bits(link.read_generation)!=32) $fatal(1,"read_generation width");
    if ($bits(link.read_request_id)!=64) $fatal(1,"read_request_id width");
    if ($bits(link.read_offset)!=64) $fatal(1,"read_offset width");
    if ($bits(link.read_data)!=64) $fatal(1,"read_data width");
    if ($bits(link.read_keep)!=8) $fatal(1,"read_keep width");
    if ($bits(link.read_last)!=1) $fatal(1,"read_last width");
    if ($bits(link.control_req_valid)!=1) $fatal(1,"control_req_valid width");
    if ($bits(link.control_req_ready)!=1) $fatal(1,"control_req_ready width");
    if ($bits(link.control_req_owner_id)!=32) $fatal(1,"control_req_owner_id width");
    if ($bits(link.control_req_instance_id)!=16) $fatal(1,"control_req_instance_id width");
    if ($bits(link.control_req_epoch)!=32) $fatal(1,"control_req_epoch width");
    if ($bits(link.control_req_task_id)!=32) $fatal(1,"control_req_task_id width");
    if ($bits(link.control_req_request_seq)!=64) $fatal(1,"control_req_request_seq width");
    if ($bits(link.control_req_context_id)!=16) $fatal(1,"control_req_context_id width");
    if ($bits(link.control_req_generation)!=32) $fatal(1,"control_req_generation width");
    if ($bits(link.control_req_service_version)!=16) $fatal(1,"control_req_service_version width");
    if ($bits(link.control_req_request_id)!=64) $fatal(1,"control_req_request_id width");
    if ($bits(link.control_req_policy_epoch)!=32) $fatal(1,"control_req_policy_epoch width");
    if ($bits(link.control_req_key_epoch)!=32) $fatal(1,"control_req_key_epoch width");
    if ($bits(link.control_req_role)!=8) $fatal(1,"control_req_role width");
    if ($bits(link.control_req_object_ref)!=64) $fatal(1,"control_req_object_ref width");
    if ($bits(link.control_req_offset)!=64) $fatal(1,"control_req_offset width");
    if ($bits(link.control_req_length)!=64) $fatal(1,"control_req_length width");
    if ($bits(link.control_req_operation)!=16) $fatal(1,"control_req_operation width");
    if ($bits(link.control_req_mode)!=8) $fatal(1,"control_req_mode width");
    if ($bits(link.control_req_use)!=16) $fatal(1,"control_req_use width");
    if ($bits(link.control_req_result_sink_ref)!=64) $fatal(1,"control_req_result_sink_ref width");
    if ($bits(link.control_req_usage_amount)!=64) $fatal(1,"control_req_usage_amount width");
    if ($bits(link.control_rsp_valid)!=1) $fatal(1,"control_rsp_valid width");
    if ($bits(link.control_rsp_ready)!=1) $fatal(1,"control_rsp_ready width");
    if ($bits(link.control_rsp_owner_id)!=32) $fatal(1,"control_rsp_owner_id width");
    if ($bits(link.control_rsp_instance_id)!=16) $fatal(1,"control_rsp_instance_id width");
    if ($bits(link.control_rsp_epoch)!=32) $fatal(1,"control_rsp_epoch width");
    if ($bits(link.control_rsp_task_id)!=32) $fatal(1,"control_rsp_task_id width");
    if ($bits(link.control_rsp_request_seq)!=64) $fatal(1,"control_rsp_request_seq width");
    if ($bits(link.control_rsp_context_id)!=16) $fatal(1,"control_rsp_context_id width");
    if ($bits(link.control_rsp_generation)!=32) $fatal(1,"control_rsp_generation width");
    if ($bits(link.control_rsp_request_id)!=64) $fatal(1,"control_rsp_request_id width");
    if ($bits(link.control_rsp_status)!=16) $fatal(1,"control_rsp_status width");
    if ($bits(link.control_rsp_object_state)!=8) $fatal(1,"control_rsp_object_state width");
    if ($bits(link.control_rsp_cleanup_done)!=1) $fatal(1,"control_rsp_cleanup_done width");
    if ($bits(aix_crypto_secret_w64_req_payload_t)!=736) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w64_rsp_payload_t)!=569) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w64_write_payload_t)!=425) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w64_read_payload_t)!=425) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w64_control_req_payload_t)!=736) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w64_control_rsp_payload_t)!=313) $fatal(1,"payload width");
    if (ROLE_KEY_LOAD!=1) $fatal(1,"ROLE_KEY_LOAD encoding");
    if (ROLE_SECRET_MESSAGE_READ!=2) $fatal(1,"ROLE_SECRET_MESSAGE_READ encoding");
    if (ROLE_RESULT_PREPARE!=3) $fatal(1,"ROLE_RESULT_PREPARE encoding");
    if (ROLE_RESULT_WRITE!=4) $fatal(1,"ROLE_RESULT_WRITE encoding");
    if (ROLE_RESULT_COMMIT!=5) $fatal(1,"ROLE_RESULT_COMMIT encoding");
    if (ROLE_RESULT_ABORT!=6) $fatal(1,"ROLE_RESULT_ABORT encoding");
    if (ROLE_REVOKE!=7) $fatal(1,"ROLE_REVOKE encoding");
    if (ROLE_USAGE_RESERVE!=8) $fatal(1,"ROLE_USAGE_RESERVE encoding");
    if (ROLE_QUERY!=9) $fatal(1,"ROLE_QUERY encoding");
    if (ROLE_NONCE_RESERVE!=10) $fatal(1,"ROLE_NONCE_RESERVE encoding");
    if (USE_MAC!=1) $fatal(1,"USE_MAC encoding");
    if (USE_ENCRYPT!=2) $fatal(1,"USE_ENCRYPT encoding");
    if (USE_DECRYPT!=3) $fatal(1,"USE_DECRYPT encoding");
    if (USE_DERIVE!=4) $fatal(1,"USE_DERIVE encoding");
    if (USE_WRAP!=5) $fatal(1,"USE_WRAP encoding");
    if (USE_UNWRAP!=6) $fatal(1,"USE_UNWRAP encoding");
    if (USE_HP!=7) $fatal(1,"USE_HP encoding");
    if (OBJECT_STATE_NONE!=0) $fatal(1,"OBJECT_STATE_NONE encoding");
    if (OBJECT_STATE_PREPARED!=1) $fatal(1,"OBJECT_STATE_PREPARED encoding");
    if (OBJECT_STATE_WRITTEN!=2) $fatal(1,"OBJECT_STATE_WRITTEN encoding");
    if (OBJECT_STATE_ACTIVE!=3) $fatal(1,"OBJECT_STATE_ACTIVE encoding");
    if (OBJECT_STATE_ABORTED!=4) $fatal(1,"OBJECT_STATE_ABORTED encoding");
    if (OBJECT_STATE_UNKNOWN!=5) $fatal(1,"OBJECT_STATE_UNKNOWN encoding");
    wait(reset_n); #1; compare_paths();
    request='1; expected='1; #1; compare_paths();
    for (int sample=1;sample<=128;sample++) begin
      request=1903'(pattern(sample)); expected=1313'(pattern(sample+271));
      #1; compare_paths();
    end
    for (int bitno=0;bitno<1903;bitno++) begin
      request='0; request[bitno]=1'b1; #1; compare_paths(); end
    for (int bitno=0;bitno<1313;bitno++) begin
      expected='0; expected[bitno]=1'b1; #1; compare_paths(); end
    $display("PASS crypto_secret DATA_W=64 fields=103"); done=1;
  end
endmodule
module crypto_secret_peer_128(aix_crypto_secret_if.service link,
  input logic [1384:0] response, output logic [1974:0] observed);
  assign link.req_ready = response[1384 +: 1];
  assign link.rsp_valid = response[1383 +: 1];
  assign link.rsp_owner_id = response[1351 +: 32];
  assign link.rsp_instance_id = response[1335 +: 16];
  assign link.rsp_epoch = response[1303 +: 32];
  assign link.rsp_task_id = response[1271 +: 32];
  assign link.rsp_request_seq = response[1207 +: 64];
  assign link.rsp_context_id = response[1191 +: 16];
  assign link.rsp_generation = response[1159 +: 32];
  assign link.rsp_request_id = response[1095 +: 64];
  assign link.rsp_status = response[1079 +: 16];
  assign link.rsp_object_ref = response[1015 +: 64];
  assign link.rsp_object_epoch = response[983 +: 32];
  assign link.rsp_total_length = response[919 +: 64];
  assign link.rsp_offset = response[855 +: 64];
  assign link.rsp_fragment_length = response[823 +: 32];
  assign link.rsp_end = response[822 +: 1];
  assign link.rsp_object_state = response[814 +: 8];
  assign link.write_ready = response[813 +: 1];
  assign link.read_valid = response[812 +: 1];
  assign link.read_owner_id = response[780 +: 32];
  assign link.read_instance_id = response[764 +: 16];
  assign link.read_epoch = response[732 +: 32];
  assign link.read_task_id = response[700 +: 32];
  assign link.read_request_seq = response[636 +: 64];
  assign link.read_context_id = response[620 +: 16];
  assign link.read_generation = response[588 +: 32];
  assign link.read_request_id = response[524 +: 64];
  assign link.read_offset = response[460 +: 64];
  assign link.read_data = response[332 +: 128];
  assign link.read_keep = response[316 +: 16];
  assign link.read_last = response[315 +: 1];
  assign link.control_req_ready = response[314 +: 1];
  assign link.control_rsp_valid = response[313 +: 1];
  assign link.control_rsp_owner_id = response[281 +: 32];
  assign link.control_rsp_instance_id = response[265 +: 16];
  assign link.control_rsp_epoch = response[233 +: 32];
  assign link.control_rsp_task_id = response[201 +: 32];
  assign link.control_rsp_request_seq = response[137 +: 64];
  assign link.control_rsp_context_id = response[121 +: 16];
  assign link.control_rsp_generation = response[89 +: 32];
  assign link.control_rsp_request_id = response[25 +: 64];
  assign link.control_rsp_status = response[9 +: 16];
  assign link.control_rsp_object_state = response[1 +: 8];
  assign link.control_rsp_cleanup_done = response[0 +: 1];
  assign observed = {link.req_valid, link.req_owner_id, link.req_instance_id, link.req_epoch, link.req_task_id, link.req_request_seq, link.req_context_id, link.req_generation, link.req_service_version, link.req_request_id, link.req_policy_epoch, link.req_key_epoch, link.req_role, link.req_object_ref, link.req_offset, link.req_length, link.req_operation, link.req_mode, link.req_use, link.req_result_sink_ref, link.req_usage_amount, link.rsp_ready, link.write_valid, link.write_owner_id, link.write_instance_id, link.write_epoch, link.write_task_id, link.write_request_seq, link.write_context_id, link.write_generation, link.write_request_id, link.write_offset, link.write_data, link.write_keep, link.write_last, link.read_ready, link.control_req_valid, link.control_req_owner_id, link.control_req_instance_id, link.control_req_epoch, link.control_req_task_id, link.control_req_request_seq, link.control_req_context_id, link.control_req_generation, link.control_req_service_version, link.control_req_request_id, link.control_req_policy_epoch, link.control_req_key_epoch, link.control_req_role, link.control_req_object_ref, link.control_req_offset, link.control_req_length, link.control_req_operation, link.control_req_mode, link.control_req_use, link.control_req_result_sink_ref, link.control_req_usage_amount, link.control_rsp_ready};
  always @(posedge link.clk) if (link.reset_n !== 1'b0 && link.reset_n !== 1'b1) $fatal(1,"reset modport");
endmodule
module crypto_secret_case_128(input logic clk, reset_n, output logic done);
  import aix_crypto_secret_pkg::*;
  aix_crypto_secret_w128_req_t request;
  aix_crypto_secret_w128_rsp_t response;
  logic [1384:0] expected;
  logic [1974:0] observed;
  aix_crypto_secret_if #(.DATA_W(128)) link (.clk(clk), .reset_n(reset_n));
  crypto_secret_peer_128 peer(link, expected, observed);
  aix_crypto_secret_flat_wrapper #(.DATA_W(128)) bridge(.link(link),
    .req_valid_i(request.req_valid),
    .req_ready_o(response.req_ready),
    .req_owner_id_i(request.req_owner_id),
    .req_instance_id_i(request.req_instance_id),
    .req_epoch_i(request.req_epoch),
    .req_task_id_i(request.req_task_id),
    .req_request_seq_i(request.req_request_seq),
    .req_context_id_i(request.req_context_id),
    .req_generation_i(request.req_generation),
    .req_service_version_i(request.req_service_version),
    .req_request_id_i(request.req_request_id),
    .req_policy_epoch_i(request.req_policy_epoch),
    .req_key_epoch_i(request.req_key_epoch),
    .req_role_i(request.req_role),
    .req_object_ref_i(request.req_object_ref),
    .req_offset_i(request.req_offset),
    .req_length_i(request.req_length),
    .req_operation_i(request.req_operation),
    .req_mode_i(request.req_mode),
    .req_use_i(request.req_use),
    .req_result_sink_ref_i(request.req_result_sink_ref),
    .req_usage_amount_i(request.req_usage_amount),
    .rsp_valid_o(response.rsp_valid),
    .rsp_ready_i(request.rsp_ready),
    .rsp_owner_id_o(response.rsp_owner_id),
    .rsp_instance_id_o(response.rsp_instance_id),
    .rsp_epoch_o(response.rsp_epoch),
    .rsp_task_id_o(response.rsp_task_id),
    .rsp_request_seq_o(response.rsp_request_seq),
    .rsp_context_id_o(response.rsp_context_id),
    .rsp_generation_o(response.rsp_generation),
    .rsp_request_id_o(response.rsp_request_id),
    .rsp_status_o(response.rsp_status),
    .rsp_object_ref_o(response.rsp_object_ref),
    .rsp_object_epoch_o(response.rsp_object_epoch),
    .rsp_total_length_o(response.rsp_total_length),
    .rsp_offset_o(response.rsp_offset),
    .rsp_fragment_length_o(response.rsp_fragment_length),
    .rsp_end_o(response.rsp_end),
    .rsp_object_state_o(response.rsp_object_state),
    .write_valid_i(request.write_valid),
    .write_ready_o(response.write_ready),
    .write_owner_id_i(request.write_owner_id),
    .write_instance_id_i(request.write_instance_id),
    .write_epoch_i(request.write_epoch),
    .write_task_id_i(request.write_task_id),
    .write_request_seq_i(request.write_request_seq),
    .write_context_id_i(request.write_context_id),
    .write_generation_i(request.write_generation),
    .write_request_id_i(request.write_request_id),
    .write_offset_i(request.write_offset),
    .write_data_i(request.write_data),
    .write_keep_i(request.write_keep),
    .write_last_i(request.write_last),
    .read_valid_o(response.read_valid),
    .read_ready_i(request.read_ready),
    .read_owner_id_o(response.read_owner_id),
    .read_instance_id_o(response.read_instance_id),
    .read_epoch_o(response.read_epoch),
    .read_task_id_o(response.read_task_id),
    .read_request_seq_o(response.read_request_seq),
    .read_context_id_o(response.read_context_id),
    .read_generation_o(response.read_generation),
    .read_request_id_o(response.read_request_id),
    .read_offset_o(response.read_offset),
    .read_data_o(response.read_data),
    .read_keep_o(response.read_keep),
    .read_last_o(response.read_last),
    .control_req_valid_i(request.control_req_valid),
    .control_req_ready_o(response.control_req_ready),
    .control_req_owner_id_i(request.control_req_owner_id),
    .control_req_instance_id_i(request.control_req_instance_id),
    .control_req_epoch_i(request.control_req_epoch),
    .control_req_task_id_i(request.control_req_task_id),
    .control_req_request_seq_i(request.control_req_request_seq),
    .control_req_context_id_i(request.control_req_context_id),
    .control_req_generation_i(request.control_req_generation),
    .control_req_service_version_i(request.control_req_service_version),
    .control_req_request_id_i(request.control_req_request_id),
    .control_req_policy_epoch_i(request.control_req_policy_epoch),
    .control_req_key_epoch_i(request.control_req_key_epoch),
    .control_req_role_i(request.control_req_role),
    .control_req_object_ref_i(request.control_req_object_ref),
    .control_req_offset_i(request.control_req_offset),
    .control_req_length_i(request.control_req_length),
    .control_req_operation_i(request.control_req_operation),
    .control_req_mode_i(request.control_req_mode),
    .control_req_use_i(request.control_req_use),
    .control_req_result_sink_ref_i(request.control_req_result_sink_ref),
    .control_req_usage_amount_i(request.control_req_usage_amount),
    .control_rsp_valid_o(response.control_rsp_valid),
    .control_rsp_ready_i(request.control_rsp_ready),
    .control_rsp_owner_id_o(response.control_rsp_owner_id),
    .control_rsp_instance_id_o(response.control_rsp_instance_id),
    .control_rsp_epoch_o(response.control_rsp_epoch),
    .control_rsp_task_id_o(response.control_rsp_task_id),
    .control_rsp_request_seq_o(response.control_rsp_request_seq),
    .control_rsp_context_id_o(response.control_rsp_context_id),
    .control_rsp_generation_o(response.control_rsp_generation),
    .control_rsp_request_id_o(response.control_rsp_request_id),
    .control_rsp_status_o(response.control_rsp_status),
    .control_rsp_object_state_o(response.control_rsp_object_state),
    .control_rsp_cleanup_done_o(response.control_rsp_cleanup_done)
  );
  function automatic logic [1974:0] pattern(input int seed);
    int unsigned state; state=32'h9e3779b9 ^ (32'h85ebca6b * seed);
    for (int i=0;i<1975;i++) begin
      state=1664525*state+1013904223; pattern[i]=state[31];
    end
  endfunction
  task automatic compare_paths;
    if (observed !== request) $fatal(1,"crypto_secret W128 forward wiring");
    if (response !== expected) $fatal(1,"crypto_secret W128 reverse wiring");
  endtask
  initial begin
    done=0; request='0; expected='0;
    if ($bits(request)!=1975 || $bits(response)!=1385) $fatal(1,"aggregate width");
    if ($bits(link.req_valid)!=1) $fatal(1,"req_valid width");
    if ($bits(link.req_ready)!=1) $fatal(1,"req_ready width");
    if ($bits(link.req_owner_id)!=32) $fatal(1,"req_owner_id width");
    if ($bits(link.req_instance_id)!=16) $fatal(1,"req_instance_id width");
    if ($bits(link.req_epoch)!=32) $fatal(1,"req_epoch width");
    if ($bits(link.req_task_id)!=32) $fatal(1,"req_task_id width");
    if ($bits(link.req_request_seq)!=64) $fatal(1,"req_request_seq width");
    if ($bits(link.req_context_id)!=16) $fatal(1,"req_context_id width");
    if ($bits(link.req_generation)!=32) $fatal(1,"req_generation width");
    if ($bits(link.req_service_version)!=16) $fatal(1,"req_service_version width");
    if ($bits(link.req_request_id)!=64) $fatal(1,"req_request_id width");
    if ($bits(link.req_policy_epoch)!=32) $fatal(1,"req_policy_epoch width");
    if ($bits(link.req_key_epoch)!=32) $fatal(1,"req_key_epoch width");
    if ($bits(link.req_role)!=8) $fatal(1,"req_role width");
    if ($bits(link.req_object_ref)!=64) $fatal(1,"req_object_ref width");
    if ($bits(link.req_offset)!=64) $fatal(1,"req_offset width");
    if ($bits(link.req_length)!=64) $fatal(1,"req_length width");
    if ($bits(link.req_operation)!=16) $fatal(1,"req_operation width");
    if ($bits(link.req_mode)!=8) $fatal(1,"req_mode width");
    if ($bits(link.req_use)!=16) $fatal(1,"req_use width");
    if ($bits(link.req_result_sink_ref)!=64) $fatal(1,"req_result_sink_ref width");
    if ($bits(link.req_usage_amount)!=64) $fatal(1,"req_usage_amount width");
    if ($bits(link.rsp_valid)!=1) $fatal(1,"rsp_valid width");
    if ($bits(link.rsp_ready)!=1) $fatal(1,"rsp_ready width");
    if ($bits(link.rsp_owner_id)!=32) $fatal(1,"rsp_owner_id width");
    if ($bits(link.rsp_instance_id)!=16) $fatal(1,"rsp_instance_id width");
    if ($bits(link.rsp_epoch)!=32) $fatal(1,"rsp_epoch width");
    if ($bits(link.rsp_task_id)!=32) $fatal(1,"rsp_task_id width");
    if ($bits(link.rsp_request_seq)!=64) $fatal(1,"rsp_request_seq width");
    if ($bits(link.rsp_context_id)!=16) $fatal(1,"rsp_context_id width");
    if ($bits(link.rsp_generation)!=32) $fatal(1,"rsp_generation width");
    if ($bits(link.rsp_request_id)!=64) $fatal(1,"rsp_request_id width");
    if ($bits(link.rsp_status)!=16) $fatal(1,"rsp_status width");
    if ($bits(link.rsp_object_ref)!=64) $fatal(1,"rsp_object_ref width");
    if ($bits(link.rsp_object_epoch)!=32) $fatal(1,"rsp_object_epoch width");
    if ($bits(link.rsp_total_length)!=64) $fatal(1,"rsp_total_length width");
    if ($bits(link.rsp_offset)!=64) $fatal(1,"rsp_offset width");
    if ($bits(link.rsp_fragment_length)!=32) $fatal(1,"rsp_fragment_length width");
    if ($bits(link.rsp_end)!=1) $fatal(1,"rsp_end width");
    if ($bits(link.rsp_object_state)!=8) $fatal(1,"rsp_object_state width");
    if ($bits(link.write_valid)!=1) $fatal(1,"write_valid width");
    if ($bits(link.write_ready)!=1) $fatal(1,"write_ready width");
    if ($bits(link.write_owner_id)!=32) $fatal(1,"write_owner_id width");
    if ($bits(link.write_instance_id)!=16) $fatal(1,"write_instance_id width");
    if ($bits(link.write_epoch)!=32) $fatal(1,"write_epoch width");
    if ($bits(link.write_task_id)!=32) $fatal(1,"write_task_id width");
    if ($bits(link.write_request_seq)!=64) $fatal(1,"write_request_seq width");
    if ($bits(link.write_context_id)!=16) $fatal(1,"write_context_id width");
    if ($bits(link.write_generation)!=32) $fatal(1,"write_generation width");
    if ($bits(link.write_request_id)!=64) $fatal(1,"write_request_id width");
    if ($bits(link.write_offset)!=64) $fatal(1,"write_offset width");
    if ($bits(link.write_data)!=128) $fatal(1,"write_data width");
    if ($bits(link.write_keep)!=16) $fatal(1,"write_keep width");
    if ($bits(link.write_last)!=1) $fatal(1,"write_last width");
    if ($bits(link.read_valid)!=1) $fatal(1,"read_valid width");
    if ($bits(link.read_ready)!=1) $fatal(1,"read_ready width");
    if ($bits(link.read_owner_id)!=32) $fatal(1,"read_owner_id width");
    if ($bits(link.read_instance_id)!=16) $fatal(1,"read_instance_id width");
    if ($bits(link.read_epoch)!=32) $fatal(1,"read_epoch width");
    if ($bits(link.read_task_id)!=32) $fatal(1,"read_task_id width");
    if ($bits(link.read_request_seq)!=64) $fatal(1,"read_request_seq width");
    if ($bits(link.read_context_id)!=16) $fatal(1,"read_context_id width");
    if ($bits(link.read_generation)!=32) $fatal(1,"read_generation width");
    if ($bits(link.read_request_id)!=64) $fatal(1,"read_request_id width");
    if ($bits(link.read_offset)!=64) $fatal(1,"read_offset width");
    if ($bits(link.read_data)!=128) $fatal(1,"read_data width");
    if ($bits(link.read_keep)!=16) $fatal(1,"read_keep width");
    if ($bits(link.read_last)!=1) $fatal(1,"read_last width");
    if ($bits(link.control_req_valid)!=1) $fatal(1,"control_req_valid width");
    if ($bits(link.control_req_ready)!=1) $fatal(1,"control_req_ready width");
    if ($bits(link.control_req_owner_id)!=32) $fatal(1,"control_req_owner_id width");
    if ($bits(link.control_req_instance_id)!=16) $fatal(1,"control_req_instance_id width");
    if ($bits(link.control_req_epoch)!=32) $fatal(1,"control_req_epoch width");
    if ($bits(link.control_req_task_id)!=32) $fatal(1,"control_req_task_id width");
    if ($bits(link.control_req_request_seq)!=64) $fatal(1,"control_req_request_seq width");
    if ($bits(link.control_req_context_id)!=16) $fatal(1,"control_req_context_id width");
    if ($bits(link.control_req_generation)!=32) $fatal(1,"control_req_generation width");
    if ($bits(link.control_req_service_version)!=16) $fatal(1,"control_req_service_version width");
    if ($bits(link.control_req_request_id)!=64) $fatal(1,"control_req_request_id width");
    if ($bits(link.control_req_policy_epoch)!=32) $fatal(1,"control_req_policy_epoch width");
    if ($bits(link.control_req_key_epoch)!=32) $fatal(1,"control_req_key_epoch width");
    if ($bits(link.control_req_role)!=8) $fatal(1,"control_req_role width");
    if ($bits(link.control_req_object_ref)!=64) $fatal(1,"control_req_object_ref width");
    if ($bits(link.control_req_offset)!=64) $fatal(1,"control_req_offset width");
    if ($bits(link.control_req_length)!=64) $fatal(1,"control_req_length width");
    if ($bits(link.control_req_operation)!=16) $fatal(1,"control_req_operation width");
    if ($bits(link.control_req_mode)!=8) $fatal(1,"control_req_mode width");
    if ($bits(link.control_req_use)!=16) $fatal(1,"control_req_use width");
    if ($bits(link.control_req_result_sink_ref)!=64) $fatal(1,"control_req_result_sink_ref width");
    if ($bits(link.control_req_usage_amount)!=64) $fatal(1,"control_req_usage_amount width");
    if ($bits(link.control_rsp_valid)!=1) $fatal(1,"control_rsp_valid width");
    if ($bits(link.control_rsp_ready)!=1) $fatal(1,"control_rsp_ready width");
    if ($bits(link.control_rsp_owner_id)!=32) $fatal(1,"control_rsp_owner_id width");
    if ($bits(link.control_rsp_instance_id)!=16) $fatal(1,"control_rsp_instance_id width");
    if ($bits(link.control_rsp_epoch)!=32) $fatal(1,"control_rsp_epoch width");
    if ($bits(link.control_rsp_task_id)!=32) $fatal(1,"control_rsp_task_id width");
    if ($bits(link.control_rsp_request_seq)!=64) $fatal(1,"control_rsp_request_seq width");
    if ($bits(link.control_rsp_context_id)!=16) $fatal(1,"control_rsp_context_id width");
    if ($bits(link.control_rsp_generation)!=32) $fatal(1,"control_rsp_generation width");
    if ($bits(link.control_rsp_request_id)!=64) $fatal(1,"control_rsp_request_id width");
    if ($bits(link.control_rsp_status)!=16) $fatal(1,"control_rsp_status width");
    if ($bits(link.control_rsp_object_state)!=8) $fatal(1,"control_rsp_object_state width");
    if ($bits(link.control_rsp_cleanup_done)!=1) $fatal(1,"control_rsp_cleanup_done width");
    if ($bits(aix_crypto_secret_w128_req_payload_t)!=736) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w128_rsp_payload_t)!=569) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w128_write_payload_t)!=497) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w128_read_payload_t)!=497) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w128_control_req_payload_t)!=736) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w128_control_rsp_payload_t)!=313) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_req_t)!=1975 || $bits(aix_crypto_secret_rsp_t)!=1385) $fatal(1,"default aliases");
    if (ROLE_KEY_LOAD!=1) $fatal(1,"ROLE_KEY_LOAD encoding");
    if (ROLE_SECRET_MESSAGE_READ!=2) $fatal(1,"ROLE_SECRET_MESSAGE_READ encoding");
    if (ROLE_RESULT_PREPARE!=3) $fatal(1,"ROLE_RESULT_PREPARE encoding");
    if (ROLE_RESULT_WRITE!=4) $fatal(1,"ROLE_RESULT_WRITE encoding");
    if (ROLE_RESULT_COMMIT!=5) $fatal(1,"ROLE_RESULT_COMMIT encoding");
    if (ROLE_RESULT_ABORT!=6) $fatal(1,"ROLE_RESULT_ABORT encoding");
    if (ROLE_REVOKE!=7) $fatal(1,"ROLE_REVOKE encoding");
    if (ROLE_USAGE_RESERVE!=8) $fatal(1,"ROLE_USAGE_RESERVE encoding");
    if (ROLE_QUERY!=9) $fatal(1,"ROLE_QUERY encoding");
    if (ROLE_NONCE_RESERVE!=10) $fatal(1,"ROLE_NONCE_RESERVE encoding");
    if (USE_MAC!=1) $fatal(1,"USE_MAC encoding");
    if (USE_ENCRYPT!=2) $fatal(1,"USE_ENCRYPT encoding");
    if (USE_DECRYPT!=3) $fatal(1,"USE_DECRYPT encoding");
    if (USE_DERIVE!=4) $fatal(1,"USE_DERIVE encoding");
    if (USE_WRAP!=5) $fatal(1,"USE_WRAP encoding");
    if (USE_UNWRAP!=6) $fatal(1,"USE_UNWRAP encoding");
    if (USE_HP!=7) $fatal(1,"USE_HP encoding");
    if (OBJECT_STATE_NONE!=0) $fatal(1,"OBJECT_STATE_NONE encoding");
    if (OBJECT_STATE_PREPARED!=1) $fatal(1,"OBJECT_STATE_PREPARED encoding");
    if (OBJECT_STATE_WRITTEN!=2) $fatal(1,"OBJECT_STATE_WRITTEN encoding");
    if (OBJECT_STATE_ACTIVE!=3) $fatal(1,"OBJECT_STATE_ACTIVE encoding");
    if (OBJECT_STATE_ABORTED!=4) $fatal(1,"OBJECT_STATE_ABORTED encoding");
    if (OBJECT_STATE_UNKNOWN!=5) $fatal(1,"OBJECT_STATE_UNKNOWN encoding");
    wait(reset_n); #1; compare_paths();
    request='1; expected='1; #1; compare_paths();
    for (int sample=1;sample<=128;sample++) begin
      request=1975'(pattern(sample)); expected=1385'(pattern(sample+271));
      #1; compare_paths();
    end
    for (int bitno=0;bitno<1975;bitno++) begin
      request='0; request[bitno]=1'b1; #1; compare_paths(); end
    for (int bitno=0;bitno<1385;bitno++) begin
      expected='0; expected[bitno]=1'b1; #1; compare_paths(); end
    $display("PASS crypto_secret DATA_W=128 fields=103"); done=1;
  end
endmodule
module crypto_secret_peer_256(aix_crypto_secret_if.service link,
  input logic [1528:0] response, output logic [2118:0] observed);
  assign link.req_ready = response[1528 +: 1];
  assign link.rsp_valid = response[1527 +: 1];
  assign link.rsp_owner_id = response[1495 +: 32];
  assign link.rsp_instance_id = response[1479 +: 16];
  assign link.rsp_epoch = response[1447 +: 32];
  assign link.rsp_task_id = response[1415 +: 32];
  assign link.rsp_request_seq = response[1351 +: 64];
  assign link.rsp_context_id = response[1335 +: 16];
  assign link.rsp_generation = response[1303 +: 32];
  assign link.rsp_request_id = response[1239 +: 64];
  assign link.rsp_status = response[1223 +: 16];
  assign link.rsp_object_ref = response[1159 +: 64];
  assign link.rsp_object_epoch = response[1127 +: 32];
  assign link.rsp_total_length = response[1063 +: 64];
  assign link.rsp_offset = response[999 +: 64];
  assign link.rsp_fragment_length = response[967 +: 32];
  assign link.rsp_end = response[966 +: 1];
  assign link.rsp_object_state = response[958 +: 8];
  assign link.write_ready = response[957 +: 1];
  assign link.read_valid = response[956 +: 1];
  assign link.read_owner_id = response[924 +: 32];
  assign link.read_instance_id = response[908 +: 16];
  assign link.read_epoch = response[876 +: 32];
  assign link.read_task_id = response[844 +: 32];
  assign link.read_request_seq = response[780 +: 64];
  assign link.read_context_id = response[764 +: 16];
  assign link.read_generation = response[732 +: 32];
  assign link.read_request_id = response[668 +: 64];
  assign link.read_offset = response[604 +: 64];
  assign link.read_data = response[348 +: 256];
  assign link.read_keep = response[316 +: 32];
  assign link.read_last = response[315 +: 1];
  assign link.control_req_ready = response[314 +: 1];
  assign link.control_rsp_valid = response[313 +: 1];
  assign link.control_rsp_owner_id = response[281 +: 32];
  assign link.control_rsp_instance_id = response[265 +: 16];
  assign link.control_rsp_epoch = response[233 +: 32];
  assign link.control_rsp_task_id = response[201 +: 32];
  assign link.control_rsp_request_seq = response[137 +: 64];
  assign link.control_rsp_context_id = response[121 +: 16];
  assign link.control_rsp_generation = response[89 +: 32];
  assign link.control_rsp_request_id = response[25 +: 64];
  assign link.control_rsp_status = response[9 +: 16];
  assign link.control_rsp_object_state = response[1 +: 8];
  assign link.control_rsp_cleanup_done = response[0 +: 1];
  assign observed = {link.req_valid, link.req_owner_id, link.req_instance_id, link.req_epoch, link.req_task_id, link.req_request_seq, link.req_context_id, link.req_generation, link.req_service_version, link.req_request_id, link.req_policy_epoch, link.req_key_epoch, link.req_role, link.req_object_ref, link.req_offset, link.req_length, link.req_operation, link.req_mode, link.req_use, link.req_result_sink_ref, link.req_usage_amount, link.rsp_ready, link.write_valid, link.write_owner_id, link.write_instance_id, link.write_epoch, link.write_task_id, link.write_request_seq, link.write_context_id, link.write_generation, link.write_request_id, link.write_offset, link.write_data, link.write_keep, link.write_last, link.read_ready, link.control_req_valid, link.control_req_owner_id, link.control_req_instance_id, link.control_req_epoch, link.control_req_task_id, link.control_req_request_seq, link.control_req_context_id, link.control_req_generation, link.control_req_service_version, link.control_req_request_id, link.control_req_policy_epoch, link.control_req_key_epoch, link.control_req_role, link.control_req_object_ref, link.control_req_offset, link.control_req_length, link.control_req_operation, link.control_req_mode, link.control_req_use, link.control_req_result_sink_ref, link.control_req_usage_amount, link.control_rsp_ready};
  always @(posedge link.clk) if (link.reset_n !== 1'b0 && link.reset_n !== 1'b1) $fatal(1,"reset modport");
endmodule
module crypto_secret_case_256(input logic clk, reset_n, output logic done);
  import aix_crypto_secret_pkg::*;
  aix_crypto_secret_w256_req_t request;
  aix_crypto_secret_w256_rsp_t response;
  logic [1528:0] expected;
  logic [2118:0] observed;
  aix_crypto_secret_if #(.DATA_W(256)) link (.clk(clk), .reset_n(reset_n));
  crypto_secret_peer_256 peer(link, expected, observed);
  aix_crypto_secret_flat_wrapper #(.DATA_W(256)) bridge(.link(link),
    .req_valid_i(request.req_valid),
    .req_ready_o(response.req_ready),
    .req_owner_id_i(request.req_owner_id),
    .req_instance_id_i(request.req_instance_id),
    .req_epoch_i(request.req_epoch),
    .req_task_id_i(request.req_task_id),
    .req_request_seq_i(request.req_request_seq),
    .req_context_id_i(request.req_context_id),
    .req_generation_i(request.req_generation),
    .req_service_version_i(request.req_service_version),
    .req_request_id_i(request.req_request_id),
    .req_policy_epoch_i(request.req_policy_epoch),
    .req_key_epoch_i(request.req_key_epoch),
    .req_role_i(request.req_role),
    .req_object_ref_i(request.req_object_ref),
    .req_offset_i(request.req_offset),
    .req_length_i(request.req_length),
    .req_operation_i(request.req_operation),
    .req_mode_i(request.req_mode),
    .req_use_i(request.req_use),
    .req_result_sink_ref_i(request.req_result_sink_ref),
    .req_usage_amount_i(request.req_usage_amount),
    .rsp_valid_o(response.rsp_valid),
    .rsp_ready_i(request.rsp_ready),
    .rsp_owner_id_o(response.rsp_owner_id),
    .rsp_instance_id_o(response.rsp_instance_id),
    .rsp_epoch_o(response.rsp_epoch),
    .rsp_task_id_o(response.rsp_task_id),
    .rsp_request_seq_o(response.rsp_request_seq),
    .rsp_context_id_o(response.rsp_context_id),
    .rsp_generation_o(response.rsp_generation),
    .rsp_request_id_o(response.rsp_request_id),
    .rsp_status_o(response.rsp_status),
    .rsp_object_ref_o(response.rsp_object_ref),
    .rsp_object_epoch_o(response.rsp_object_epoch),
    .rsp_total_length_o(response.rsp_total_length),
    .rsp_offset_o(response.rsp_offset),
    .rsp_fragment_length_o(response.rsp_fragment_length),
    .rsp_end_o(response.rsp_end),
    .rsp_object_state_o(response.rsp_object_state),
    .write_valid_i(request.write_valid),
    .write_ready_o(response.write_ready),
    .write_owner_id_i(request.write_owner_id),
    .write_instance_id_i(request.write_instance_id),
    .write_epoch_i(request.write_epoch),
    .write_task_id_i(request.write_task_id),
    .write_request_seq_i(request.write_request_seq),
    .write_context_id_i(request.write_context_id),
    .write_generation_i(request.write_generation),
    .write_request_id_i(request.write_request_id),
    .write_offset_i(request.write_offset),
    .write_data_i(request.write_data),
    .write_keep_i(request.write_keep),
    .write_last_i(request.write_last),
    .read_valid_o(response.read_valid),
    .read_ready_i(request.read_ready),
    .read_owner_id_o(response.read_owner_id),
    .read_instance_id_o(response.read_instance_id),
    .read_epoch_o(response.read_epoch),
    .read_task_id_o(response.read_task_id),
    .read_request_seq_o(response.read_request_seq),
    .read_context_id_o(response.read_context_id),
    .read_generation_o(response.read_generation),
    .read_request_id_o(response.read_request_id),
    .read_offset_o(response.read_offset),
    .read_data_o(response.read_data),
    .read_keep_o(response.read_keep),
    .read_last_o(response.read_last),
    .control_req_valid_i(request.control_req_valid),
    .control_req_ready_o(response.control_req_ready),
    .control_req_owner_id_i(request.control_req_owner_id),
    .control_req_instance_id_i(request.control_req_instance_id),
    .control_req_epoch_i(request.control_req_epoch),
    .control_req_task_id_i(request.control_req_task_id),
    .control_req_request_seq_i(request.control_req_request_seq),
    .control_req_context_id_i(request.control_req_context_id),
    .control_req_generation_i(request.control_req_generation),
    .control_req_service_version_i(request.control_req_service_version),
    .control_req_request_id_i(request.control_req_request_id),
    .control_req_policy_epoch_i(request.control_req_policy_epoch),
    .control_req_key_epoch_i(request.control_req_key_epoch),
    .control_req_role_i(request.control_req_role),
    .control_req_object_ref_i(request.control_req_object_ref),
    .control_req_offset_i(request.control_req_offset),
    .control_req_length_i(request.control_req_length),
    .control_req_operation_i(request.control_req_operation),
    .control_req_mode_i(request.control_req_mode),
    .control_req_use_i(request.control_req_use),
    .control_req_result_sink_ref_i(request.control_req_result_sink_ref),
    .control_req_usage_amount_i(request.control_req_usage_amount),
    .control_rsp_valid_o(response.control_rsp_valid),
    .control_rsp_ready_i(request.control_rsp_ready),
    .control_rsp_owner_id_o(response.control_rsp_owner_id),
    .control_rsp_instance_id_o(response.control_rsp_instance_id),
    .control_rsp_epoch_o(response.control_rsp_epoch),
    .control_rsp_task_id_o(response.control_rsp_task_id),
    .control_rsp_request_seq_o(response.control_rsp_request_seq),
    .control_rsp_context_id_o(response.control_rsp_context_id),
    .control_rsp_generation_o(response.control_rsp_generation),
    .control_rsp_request_id_o(response.control_rsp_request_id),
    .control_rsp_status_o(response.control_rsp_status),
    .control_rsp_object_state_o(response.control_rsp_object_state),
    .control_rsp_cleanup_done_o(response.control_rsp_cleanup_done)
  );
  function automatic logic [2118:0] pattern(input int seed);
    int unsigned state; state=32'h9e3779b9 ^ (32'h85ebca6b * seed);
    for (int i=0;i<2119;i++) begin
      state=1664525*state+1013904223; pattern[i]=state[31];
    end
  endfunction
  task automatic compare_paths;
    if (observed !== request) $fatal(1,"crypto_secret W256 forward wiring");
    if (response !== expected) $fatal(1,"crypto_secret W256 reverse wiring");
  endtask
  initial begin
    done=0; request='0; expected='0;
    if ($bits(request)!=2119 || $bits(response)!=1529) $fatal(1,"aggregate width");
    if ($bits(link.req_valid)!=1) $fatal(1,"req_valid width");
    if ($bits(link.req_ready)!=1) $fatal(1,"req_ready width");
    if ($bits(link.req_owner_id)!=32) $fatal(1,"req_owner_id width");
    if ($bits(link.req_instance_id)!=16) $fatal(1,"req_instance_id width");
    if ($bits(link.req_epoch)!=32) $fatal(1,"req_epoch width");
    if ($bits(link.req_task_id)!=32) $fatal(1,"req_task_id width");
    if ($bits(link.req_request_seq)!=64) $fatal(1,"req_request_seq width");
    if ($bits(link.req_context_id)!=16) $fatal(1,"req_context_id width");
    if ($bits(link.req_generation)!=32) $fatal(1,"req_generation width");
    if ($bits(link.req_service_version)!=16) $fatal(1,"req_service_version width");
    if ($bits(link.req_request_id)!=64) $fatal(1,"req_request_id width");
    if ($bits(link.req_policy_epoch)!=32) $fatal(1,"req_policy_epoch width");
    if ($bits(link.req_key_epoch)!=32) $fatal(1,"req_key_epoch width");
    if ($bits(link.req_role)!=8) $fatal(1,"req_role width");
    if ($bits(link.req_object_ref)!=64) $fatal(1,"req_object_ref width");
    if ($bits(link.req_offset)!=64) $fatal(1,"req_offset width");
    if ($bits(link.req_length)!=64) $fatal(1,"req_length width");
    if ($bits(link.req_operation)!=16) $fatal(1,"req_operation width");
    if ($bits(link.req_mode)!=8) $fatal(1,"req_mode width");
    if ($bits(link.req_use)!=16) $fatal(1,"req_use width");
    if ($bits(link.req_result_sink_ref)!=64) $fatal(1,"req_result_sink_ref width");
    if ($bits(link.req_usage_amount)!=64) $fatal(1,"req_usage_amount width");
    if ($bits(link.rsp_valid)!=1) $fatal(1,"rsp_valid width");
    if ($bits(link.rsp_ready)!=1) $fatal(1,"rsp_ready width");
    if ($bits(link.rsp_owner_id)!=32) $fatal(1,"rsp_owner_id width");
    if ($bits(link.rsp_instance_id)!=16) $fatal(1,"rsp_instance_id width");
    if ($bits(link.rsp_epoch)!=32) $fatal(1,"rsp_epoch width");
    if ($bits(link.rsp_task_id)!=32) $fatal(1,"rsp_task_id width");
    if ($bits(link.rsp_request_seq)!=64) $fatal(1,"rsp_request_seq width");
    if ($bits(link.rsp_context_id)!=16) $fatal(1,"rsp_context_id width");
    if ($bits(link.rsp_generation)!=32) $fatal(1,"rsp_generation width");
    if ($bits(link.rsp_request_id)!=64) $fatal(1,"rsp_request_id width");
    if ($bits(link.rsp_status)!=16) $fatal(1,"rsp_status width");
    if ($bits(link.rsp_object_ref)!=64) $fatal(1,"rsp_object_ref width");
    if ($bits(link.rsp_object_epoch)!=32) $fatal(1,"rsp_object_epoch width");
    if ($bits(link.rsp_total_length)!=64) $fatal(1,"rsp_total_length width");
    if ($bits(link.rsp_offset)!=64) $fatal(1,"rsp_offset width");
    if ($bits(link.rsp_fragment_length)!=32) $fatal(1,"rsp_fragment_length width");
    if ($bits(link.rsp_end)!=1) $fatal(1,"rsp_end width");
    if ($bits(link.rsp_object_state)!=8) $fatal(1,"rsp_object_state width");
    if ($bits(link.write_valid)!=1) $fatal(1,"write_valid width");
    if ($bits(link.write_ready)!=1) $fatal(1,"write_ready width");
    if ($bits(link.write_owner_id)!=32) $fatal(1,"write_owner_id width");
    if ($bits(link.write_instance_id)!=16) $fatal(1,"write_instance_id width");
    if ($bits(link.write_epoch)!=32) $fatal(1,"write_epoch width");
    if ($bits(link.write_task_id)!=32) $fatal(1,"write_task_id width");
    if ($bits(link.write_request_seq)!=64) $fatal(1,"write_request_seq width");
    if ($bits(link.write_context_id)!=16) $fatal(1,"write_context_id width");
    if ($bits(link.write_generation)!=32) $fatal(1,"write_generation width");
    if ($bits(link.write_request_id)!=64) $fatal(1,"write_request_id width");
    if ($bits(link.write_offset)!=64) $fatal(1,"write_offset width");
    if ($bits(link.write_data)!=256) $fatal(1,"write_data width");
    if ($bits(link.write_keep)!=32) $fatal(1,"write_keep width");
    if ($bits(link.write_last)!=1) $fatal(1,"write_last width");
    if ($bits(link.read_valid)!=1) $fatal(1,"read_valid width");
    if ($bits(link.read_ready)!=1) $fatal(1,"read_ready width");
    if ($bits(link.read_owner_id)!=32) $fatal(1,"read_owner_id width");
    if ($bits(link.read_instance_id)!=16) $fatal(1,"read_instance_id width");
    if ($bits(link.read_epoch)!=32) $fatal(1,"read_epoch width");
    if ($bits(link.read_task_id)!=32) $fatal(1,"read_task_id width");
    if ($bits(link.read_request_seq)!=64) $fatal(1,"read_request_seq width");
    if ($bits(link.read_context_id)!=16) $fatal(1,"read_context_id width");
    if ($bits(link.read_generation)!=32) $fatal(1,"read_generation width");
    if ($bits(link.read_request_id)!=64) $fatal(1,"read_request_id width");
    if ($bits(link.read_offset)!=64) $fatal(1,"read_offset width");
    if ($bits(link.read_data)!=256) $fatal(1,"read_data width");
    if ($bits(link.read_keep)!=32) $fatal(1,"read_keep width");
    if ($bits(link.read_last)!=1) $fatal(1,"read_last width");
    if ($bits(link.control_req_valid)!=1) $fatal(1,"control_req_valid width");
    if ($bits(link.control_req_ready)!=1) $fatal(1,"control_req_ready width");
    if ($bits(link.control_req_owner_id)!=32) $fatal(1,"control_req_owner_id width");
    if ($bits(link.control_req_instance_id)!=16) $fatal(1,"control_req_instance_id width");
    if ($bits(link.control_req_epoch)!=32) $fatal(1,"control_req_epoch width");
    if ($bits(link.control_req_task_id)!=32) $fatal(1,"control_req_task_id width");
    if ($bits(link.control_req_request_seq)!=64) $fatal(1,"control_req_request_seq width");
    if ($bits(link.control_req_context_id)!=16) $fatal(1,"control_req_context_id width");
    if ($bits(link.control_req_generation)!=32) $fatal(1,"control_req_generation width");
    if ($bits(link.control_req_service_version)!=16) $fatal(1,"control_req_service_version width");
    if ($bits(link.control_req_request_id)!=64) $fatal(1,"control_req_request_id width");
    if ($bits(link.control_req_policy_epoch)!=32) $fatal(1,"control_req_policy_epoch width");
    if ($bits(link.control_req_key_epoch)!=32) $fatal(1,"control_req_key_epoch width");
    if ($bits(link.control_req_role)!=8) $fatal(1,"control_req_role width");
    if ($bits(link.control_req_object_ref)!=64) $fatal(1,"control_req_object_ref width");
    if ($bits(link.control_req_offset)!=64) $fatal(1,"control_req_offset width");
    if ($bits(link.control_req_length)!=64) $fatal(1,"control_req_length width");
    if ($bits(link.control_req_operation)!=16) $fatal(1,"control_req_operation width");
    if ($bits(link.control_req_mode)!=8) $fatal(1,"control_req_mode width");
    if ($bits(link.control_req_use)!=16) $fatal(1,"control_req_use width");
    if ($bits(link.control_req_result_sink_ref)!=64) $fatal(1,"control_req_result_sink_ref width");
    if ($bits(link.control_req_usage_amount)!=64) $fatal(1,"control_req_usage_amount width");
    if ($bits(link.control_rsp_valid)!=1) $fatal(1,"control_rsp_valid width");
    if ($bits(link.control_rsp_ready)!=1) $fatal(1,"control_rsp_ready width");
    if ($bits(link.control_rsp_owner_id)!=32) $fatal(1,"control_rsp_owner_id width");
    if ($bits(link.control_rsp_instance_id)!=16) $fatal(1,"control_rsp_instance_id width");
    if ($bits(link.control_rsp_epoch)!=32) $fatal(1,"control_rsp_epoch width");
    if ($bits(link.control_rsp_task_id)!=32) $fatal(1,"control_rsp_task_id width");
    if ($bits(link.control_rsp_request_seq)!=64) $fatal(1,"control_rsp_request_seq width");
    if ($bits(link.control_rsp_context_id)!=16) $fatal(1,"control_rsp_context_id width");
    if ($bits(link.control_rsp_generation)!=32) $fatal(1,"control_rsp_generation width");
    if ($bits(link.control_rsp_request_id)!=64) $fatal(1,"control_rsp_request_id width");
    if ($bits(link.control_rsp_status)!=16) $fatal(1,"control_rsp_status width");
    if ($bits(link.control_rsp_object_state)!=8) $fatal(1,"control_rsp_object_state width");
    if ($bits(link.control_rsp_cleanup_done)!=1) $fatal(1,"control_rsp_cleanup_done width");
    if ($bits(aix_crypto_secret_w256_req_payload_t)!=736) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w256_rsp_payload_t)!=569) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w256_write_payload_t)!=641) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w256_read_payload_t)!=641) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w256_control_req_payload_t)!=736) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w256_control_rsp_payload_t)!=313) $fatal(1,"payload width");
    if (ROLE_KEY_LOAD!=1) $fatal(1,"ROLE_KEY_LOAD encoding");
    if (ROLE_SECRET_MESSAGE_READ!=2) $fatal(1,"ROLE_SECRET_MESSAGE_READ encoding");
    if (ROLE_RESULT_PREPARE!=3) $fatal(1,"ROLE_RESULT_PREPARE encoding");
    if (ROLE_RESULT_WRITE!=4) $fatal(1,"ROLE_RESULT_WRITE encoding");
    if (ROLE_RESULT_COMMIT!=5) $fatal(1,"ROLE_RESULT_COMMIT encoding");
    if (ROLE_RESULT_ABORT!=6) $fatal(1,"ROLE_RESULT_ABORT encoding");
    if (ROLE_REVOKE!=7) $fatal(1,"ROLE_REVOKE encoding");
    if (ROLE_USAGE_RESERVE!=8) $fatal(1,"ROLE_USAGE_RESERVE encoding");
    if (ROLE_QUERY!=9) $fatal(1,"ROLE_QUERY encoding");
    if (ROLE_NONCE_RESERVE!=10) $fatal(1,"ROLE_NONCE_RESERVE encoding");
    if (USE_MAC!=1) $fatal(1,"USE_MAC encoding");
    if (USE_ENCRYPT!=2) $fatal(1,"USE_ENCRYPT encoding");
    if (USE_DECRYPT!=3) $fatal(1,"USE_DECRYPT encoding");
    if (USE_DERIVE!=4) $fatal(1,"USE_DERIVE encoding");
    if (USE_WRAP!=5) $fatal(1,"USE_WRAP encoding");
    if (USE_UNWRAP!=6) $fatal(1,"USE_UNWRAP encoding");
    if (USE_HP!=7) $fatal(1,"USE_HP encoding");
    if (OBJECT_STATE_NONE!=0) $fatal(1,"OBJECT_STATE_NONE encoding");
    if (OBJECT_STATE_PREPARED!=1) $fatal(1,"OBJECT_STATE_PREPARED encoding");
    if (OBJECT_STATE_WRITTEN!=2) $fatal(1,"OBJECT_STATE_WRITTEN encoding");
    if (OBJECT_STATE_ACTIVE!=3) $fatal(1,"OBJECT_STATE_ACTIVE encoding");
    if (OBJECT_STATE_ABORTED!=4) $fatal(1,"OBJECT_STATE_ABORTED encoding");
    if (OBJECT_STATE_UNKNOWN!=5) $fatal(1,"OBJECT_STATE_UNKNOWN encoding");
    wait(reset_n); #1; compare_paths();
    request='1; expected='1; #1; compare_paths();
    for (int sample=1;sample<=128;sample++) begin
      request=2119'(pattern(sample)); expected=1529'(pattern(sample+271));
      #1; compare_paths();
    end
    for (int bitno=0;bitno<2119;bitno++) begin
      request='0; request[bitno]=1'b1; #1; compare_paths(); end
    for (int bitno=0;bitno<1529;bitno++) begin
      expected='0; expected[bitno]=1'b1; #1; compare_paths(); end
    $display("PASS crypto_secret DATA_W=256 fields=103"); done=1;
  end
endmodule
module crypto_secret_peer_512(aix_crypto_secret_if.service link,
  input logic [1816:0] response, output logic [2406:0] observed);
  assign link.req_ready = response[1816 +: 1];
  assign link.rsp_valid = response[1815 +: 1];
  assign link.rsp_owner_id = response[1783 +: 32];
  assign link.rsp_instance_id = response[1767 +: 16];
  assign link.rsp_epoch = response[1735 +: 32];
  assign link.rsp_task_id = response[1703 +: 32];
  assign link.rsp_request_seq = response[1639 +: 64];
  assign link.rsp_context_id = response[1623 +: 16];
  assign link.rsp_generation = response[1591 +: 32];
  assign link.rsp_request_id = response[1527 +: 64];
  assign link.rsp_status = response[1511 +: 16];
  assign link.rsp_object_ref = response[1447 +: 64];
  assign link.rsp_object_epoch = response[1415 +: 32];
  assign link.rsp_total_length = response[1351 +: 64];
  assign link.rsp_offset = response[1287 +: 64];
  assign link.rsp_fragment_length = response[1255 +: 32];
  assign link.rsp_end = response[1254 +: 1];
  assign link.rsp_object_state = response[1246 +: 8];
  assign link.write_ready = response[1245 +: 1];
  assign link.read_valid = response[1244 +: 1];
  assign link.read_owner_id = response[1212 +: 32];
  assign link.read_instance_id = response[1196 +: 16];
  assign link.read_epoch = response[1164 +: 32];
  assign link.read_task_id = response[1132 +: 32];
  assign link.read_request_seq = response[1068 +: 64];
  assign link.read_context_id = response[1052 +: 16];
  assign link.read_generation = response[1020 +: 32];
  assign link.read_request_id = response[956 +: 64];
  assign link.read_offset = response[892 +: 64];
  assign link.read_data = response[380 +: 512];
  assign link.read_keep = response[316 +: 64];
  assign link.read_last = response[315 +: 1];
  assign link.control_req_ready = response[314 +: 1];
  assign link.control_rsp_valid = response[313 +: 1];
  assign link.control_rsp_owner_id = response[281 +: 32];
  assign link.control_rsp_instance_id = response[265 +: 16];
  assign link.control_rsp_epoch = response[233 +: 32];
  assign link.control_rsp_task_id = response[201 +: 32];
  assign link.control_rsp_request_seq = response[137 +: 64];
  assign link.control_rsp_context_id = response[121 +: 16];
  assign link.control_rsp_generation = response[89 +: 32];
  assign link.control_rsp_request_id = response[25 +: 64];
  assign link.control_rsp_status = response[9 +: 16];
  assign link.control_rsp_object_state = response[1 +: 8];
  assign link.control_rsp_cleanup_done = response[0 +: 1];
  assign observed = {link.req_valid, link.req_owner_id, link.req_instance_id, link.req_epoch, link.req_task_id, link.req_request_seq, link.req_context_id, link.req_generation, link.req_service_version, link.req_request_id, link.req_policy_epoch, link.req_key_epoch, link.req_role, link.req_object_ref, link.req_offset, link.req_length, link.req_operation, link.req_mode, link.req_use, link.req_result_sink_ref, link.req_usage_amount, link.rsp_ready, link.write_valid, link.write_owner_id, link.write_instance_id, link.write_epoch, link.write_task_id, link.write_request_seq, link.write_context_id, link.write_generation, link.write_request_id, link.write_offset, link.write_data, link.write_keep, link.write_last, link.read_ready, link.control_req_valid, link.control_req_owner_id, link.control_req_instance_id, link.control_req_epoch, link.control_req_task_id, link.control_req_request_seq, link.control_req_context_id, link.control_req_generation, link.control_req_service_version, link.control_req_request_id, link.control_req_policy_epoch, link.control_req_key_epoch, link.control_req_role, link.control_req_object_ref, link.control_req_offset, link.control_req_length, link.control_req_operation, link.control_req_mode, link.control_req_use, link.control_req_result_sink_ref, link.control_req_usage_amount, link.control_rsp_ready};
  always @(posedge link.clk) if (link.reset_n !== 1'b0 && link.reset_n !== 1'b1) $fatal(1,"reset modport");
endmodule
module crypto_secret_case_512(input logic clk, reset_n, output logic done);
  import aix_crypto_secret_pkg::*;
  aix_crypto_secret_w512_req_t request;
  aix_crypto_secret_w512_rsp_t response;
  logic [1816:0] expected;
  logic [2406:0] observed;
  aix_crypto_secret_if #(.DATA_W(512)) link (.clk(clk), .reset_n(reset_n));
  crypto_secret_peer_512 peer(link, expected, observed);
  aix_crypto_secret_flat_wrapper #(.DATA_W(512)) bridge(.link(link),
    .req_valid_i(request.req_valid),
    .req_ready_o(response.req_ready),
    .req_owner_id_i(request.req_owner_id),
    .req_instance_id_i(request.req_instance_id),
    .req_epoch_i(request.req_epoch),
    .req_task_id_i(request.req_task_id),
    .req_request_seq_i(request.req_request_seq),
    .req_context_id_i(request.req_context_id),
    .req_generation_i(request.req_generation),
    .req_service_version_i(request.req_service_version),
    .req_request_id_i(request.req_request_id),
    .req_policy_epoch_i(request.req_policy_epoch),
    .req_key_epoch_i(request.req_key_epoch),
    .req_role_i(request.req_role),
    .req_object_ref_i(request.req_object_ref),
    .req_offset_i(request.req_offset),
    .req_length_i(request.req_length),
    .req_operation_i(request.req_operation),
    .req_mode_i(request.req_mode),
    .req_use_i(request.req_use),
    .req_result_sink_ref_i(request.req_result_sink_ref),
    .req_usage_amount_i(request.req_usage_amount),
    .rsp_valid_o(response.rsp_valid),
    .rsp_ready_i(request.rsp_ready),
    .rsp_owner_id_o(response.rsp_owner_id),
    .rsp_instance_id_o(response.rsp_instance_id),
    .rsp_epoch_o(response.rsp_epoch),
    .rsp_task_id_o(response.rsp_task_id),
    .rsp_request_seq_o(response.rsp_request_seq),
    .rsp_context_id_o(response.rsp_context_id),
    .rsp_generation_o(response.rsp_generation),
    .rsp_request_id_o(response.rsp_request_id),
    .rsp_status_o(response.rsp_status),
    .rsp_object_ref_o(response.rsp_object_ref),
    .rsp_object_epoch_o(response.rsp_object_epoch),
    .rsp_total_length_o(response.rsp_total_length),
    .rsp_offset_o(response.rsp_offset),
    .rsp_fragment_length_o(response.rsp_fragment_length),
    .rsp_end_o(response.rsp_end),
    .rsp_object_state_o(response.rsp_object_state),
    .write_valid_i(request.write_valid),
    .write_ready_o(response.write_ready),
    .write_owner_id_i(request.write_owner_id),
    .write_instance_id_i(request.write_instance_id),
    .write_epoch_i(request.write_epoch),
    .write_task_id_i(request.write_task_id),
    .write_request_seq_i(request.write_request_seq),
    .write_context_id_i(request.write_context_id),
    .write_generation_i(request.write_generation),
    .write_request_id_i(request.write_request_id),
    .write_offset_i(request.write_offset),
    .write_data_i(request.write_data),
    .write_keep_i(request.write_keep),
    .write_last_i(request.write_last),
    .read_valid_o(response.read_valid),
    .read_ready_i(request.read_ready),
    .read_owner_id_o(response.read_owner_id),
    .read_instance_id_o(response.read_instance_id),
    .read_epoch_o(response.read_epoch),
    .read_task_id_o(response.read_task_id),
    .read_request_seq_o(response.read_request_seq),
    .read_context_id_o(response.read_context_id),
    .read_generation_o(response.read_generation),
    .read_request_id_o(response.read_request_id),
    .read_offset_o(response.read_offset),
    .read_data_o(response.read_data),
    .read_keep_o(response.read_keep),
    .read_last_o(response.read_last),
    .control_req_valid_i(request.control_req_valid),
    .control_req_ready_o(response.control_req_ready),
    .control_req_owner_id_i(request.control_req_owner_id),
    .control_req_instance_id_i(request.control_req_instance_id),
    .control_req_epoch_i(request.control_req_epoch),
    .control_req_task_id_i(request.control_req_task_id),
    .control_req_request_seq_i(request.control_req_request_seq),
    .control_req_context_id_i(request.control_req_context_id),
    .control_req_generation_i(request.control_req_generation),
    .control_req_service_version_i(request.control_req_service_version),
    .control_req_request_id_i(request.control_req_request_id),
    .control_req_policy_epoch_i(request.control_req_policy_epoch),
    .control_req_key_epoch_i(request.control_req_key_epoch),
    .control_req_role_i(request.control_req_role),
    .control_req_object_ref_i(request.control_req_object_ref),
    .control_req_offset_i(request.control_req_offset),
    .control_req_length_i(request.control_req_length),
    .control_req_operation_i(request.control_req_operation),
    .control_req_mode_i(request.control_req_mode),
    .control_req_use_i(request.control_req_use),
    .control_req_result_sink_ref_i(request.control_req_result_sink_ref),
    .control_req_usage_amount_i(request.control_req_usage_amount),
    .control_rsp_valid_o(response.control_rsp_valid),
    .control_rsp_ready_i(request.control_rsp_ready),
    .control_rsp_owner_id_o(response.control_rsp_owner_id),
    .control_rsp_instance_id_o(response.control_rsp_instance_id),
    .control_rsp_epoch_o(response.control_rsp_epoch),
    .control_rsp_task_id_o(response.control_rsp_task_id),
    .control_rsp_request_seq_o(response.control_rsp_request_seq),
    .control_rsp_context_id_o(response.control_rsp_context_id),
    .control_rsp_generation_o(response.control_rsp_generation),
    .control_rsp_request_id_o(response.control_rsp_request_id),
    .control_rsp_status_o(response.control_rsp_status),
    .control_rsp_object_state_o(response.control_rsp_object_state),
    .control_rsp_cleanup_done_o(response.control_rsp_cleanup_done)
  );
  function automatic logic [2406:0] pattern(input int seed);
    int unsigned state; state=32'h9e3779b9 ^ (32'h85ebca6b * seed);
    for (int i=0;i<2407;i++) begin
      state=1664525*state+1013904223; pattern[i]=state[31];
    end
  endfunction
  task automatic compare_paths;
    if (observed !== request) $fatal(1,"crypto_secret W512 forward wiring");
    if (response !== expected) $fatal(1,"crypto_secret W512 reverse wiring");
  endtask
  initial begin
    done=0; request='0; expected='0;
    if ($bits(request)!=2407 || $bits(response)!=1817) $fatal(1,"aggregate width");
    if ($bits(link.req_valid)!=1) $fatal(1,"req_valid width");
    if ($bits(link.req_ready)!=1) $fatal(1,"req_ready width");
    if ($bits(link.req_owner_id)!=32) $fatal(1,"req_owner_id width");
    if ($bits(link.req_instance_id)!=16) $fatal(1,"req_instance_id width");
    if ($bits(link.req_epoch)!=32) $fatal(1,"req_epoch width");
    if ($bits(link.req_task_id)!=32) $fatal(1,"req_task_id width");
    if ($bits(link.req_request_seq)!=64) $fatal(1,"req_request_seq width");
    if ($bits(link.req_context_id)!=16) $fatal(1,"req_context_id width");
    if ($bits(link.req_generation)!=32) $fatal(1,"req_generation width");
    if ($bits(link.req_service_version)!=16) $fatal(1,"req_service_version width");
    if ($bits(link.req_request_id)!=64) $fatal(1,"req_request_id width");
    if ($bits(link.req_policy_epoch)!=32) $fatal(1,"req_policy_epoch width");
    if ($bits(link.req_key_epoch)!=32) $fatal(1,"req_key_epoch width");
    if ($bits(link.req_role)!=8) $fatal(1,"req_role width");
    if ($bits(link.req_object_ref)!=64) $fatal(1,"req_object_ref width");
    if ($bits(link.req_offset)!=64) $fatal(1,"req_offset width");
    if ($bits(link.req_length)!=64) $fatal(1,"req_length width");
    if ($bits(link.req_operation)!=16) $fatal(1,"req_operation width");
    if ($bits(link.req_mode)!=8) $fatal(1,"req_mode width");
    if ($bits(link.req_use)!=16) $fatal(1,"req_use width");
    if ($bits(link.req_result_sink_ref)!=64) $fatal(1,"req_result_sink_ref width");
    if ($bits(link.req_usage_amount)!=64) $fatal(1,"req_usage_amount width");
    if ($bits(link.rsp_valid)!=1) $fatal(1,"rsp_valid width");
    if ($bits(link.rsp_ready)!=1) $fatal(1,"rsp_ready width");
    if ($bits(link.rsp_owner_id)!=32) $fatal(1,"rsp_owner_id width");
    if ($bits(link.rsp_instance_id)!=16) $fatal(1,"rsp_instance_id width");
    if ($bits(link.rsp_epoch)!=32) $fatal(1,"rsp_epoch width");
    if ($bits(link.rsp_task_id)!=32) $fatal(1,"rsp_task_id width");
    if ($bits(link.rsp_request_seq)!=64) $fatal(1,"rsp_request_seq width");
    if ($bits(link.rsp_context_id)!=16) $fatal(1,"rsp_context_id width");
    if ($bits(link.rsp_generation)!=32) $fatal(1,"rsp_generation width");
    if ($bits(link.rsp_request_id)!=64) $fatal(1,"rsp_request_id width");
    if ($bits(link.rsp_status)!=16) $fatal(1,"rsp_status width");
    if ($bits(link.rsp_object_ref)!=64) $fatal(1,"rsp_object_ref width");
    if ($bits(link.rsp_object_epoch)!=32) $fatal(1,"rsp_object_epoch width");
    if ($bits(link.rsp_total_length)!=64) $fatal(1,"rsp_total_length width");
    if ($bits(link.rsp_offset)!=64) $fatal(1,"rsp_offset width");
    if ($bits(link.rsp_fragment_length)!=32) $fatal(1,"rsp_fragment_length width");
    if ($bits(link.rsp_end)!=1) $fatal(1,"rsp_end width");
    if ($bits(link.rsp_object_state)!=8) $fatal(1,"rsp_object_state width");
    if ($bits(link.write_valid)!=1) $fatal(1,"write_valid width");
    if ($bits(link.write_ready)!=1) $fatal(1,"write_ready width");
    if ($bits(link.write_owner_id)!=32) $fatal(1,"write_owner_id width");
    if ($bits(link.write_instance_id)!=16) $fatal(1,"write_instance_id width");
    if ($bits(link.write_epoch)!=32) $fatal(1,"write_epoch width");
    if ($bits(link.write_task_id)!=32) $fatal(1,"write_task_id width");
    if ($bits(link.write_request_seq)!=64) $fatal(1,"write_request_seq width");
    if ($bits(link.write_context_id)!=16) $fatal(1,"write_context_id width");
    if ($bits(link.write_generation)!=32) $fatal(1,"write_generation width");
    if ($bits(link.write_request_id)!=64) $fatal(1,"write_request_id width");
    if ($bits(link.write_offset)!=64) $fatal(1,"write_offset width");
    if ($bits(link.write_data)!=512) $fatal(1,"write_data width");
    if ($bits(link.write_keep)!=64) $fatal(1,"write_keep width");
    if ($bits(link.write_last)!=1) $fatal(1,"write_last width");
    if ($bits(link.read_valid)!=1) $fatal(1,"read_valid width");
    if ($bits(link.read_ready)!=1) $fatal(1,"read_ready width");
    if ($bits(link.read_owner_id)!=32) $fatal(1,"read_owner_id width");
    if ($bits(link.read_instance_id)!=16) $fatal(1,"read_instance_id width");
    if ($bits(link.read_epoch)!=32) $fatal(1,"read_epoch width");
    if ($bits(link.read_task_id)!=32) $fatal(1,"read_task_id width");
    if ($bits(link.read_request_seq)!=64) $fatal(1,"read_request_seq width");
    if ($bits(link.read_context_id)!=16) $fatal(1,"read_context_id width");
    if ($bits(link.read_generation)!=32) $fatal(1,"read_generation width");
    if ($bits(link.read_request_id)!=64) $fatal(1,"read_request_id width");
    if ($bits(link.read_offset)!=64) $fatal(1,"read_offset width");
    if ($bits(link.read_data)!=512) $fatal(1,"read_data width");
    if ($bits(link.read_keep)!=64) $fatal(1,"read_keep width");
    if ($bits(link.read_last)!=1) $fatal(1,"read_last width");
    if ($bits(link.control_req_valid)!=1) $fatal(1,"control_req_valid width");
    if ($bits(link.control_req_ready)!=1) $fatal(1,"control_req_ready width");
    if ($bits(link.control_req_owner_id)!=32) $fatal(1,"control_req_owner_id width");
    if ($bits(link.control_req_instance_id)!=16) $fatal(1,"control_req_instance_id width");
    if ($bits(link.control_req_epoch)!=32) $fatal(1,"control_req_epoch width");
    if ($bits(link.control_req_task_id)!=32) $fatal(1,"control_req_task_id width");
    if ($bits(link.control_req_request_seq)!=64) $fatal(1,"control_req_request_seq width");
    if ($bits(link.control_req_context_id)!=16) $fatal(1,"control_req_context_id width");
    if ($bits(link.control_req_generation)!=32) $fatal(1,"control_req_generation width");
    if ($bits(link.control_req_service_version)!=16) $fatal(1,"control_req_service_version width");
    if ($bits(link.control_req_request_id)!=64) $fatal(1,"control_req_request_id width");
    if ($bits(link.control_req_policy_epoch)!=32) $fatal(1,"control_req_policy_epoch width");
    if ($bits(link.control_req_key_epoch)!=32) $fatal(1,"control_req_key_epoch width");
    if ($bits(link.control_req_role)!=8) $fatal(1,"control_req_role width");
    if ($bits(link.control_req_object_ref)!=64) $fatal(1,"control_req_object_ref width");
    if ($bits(link.control_req_offset)!=64) $fatal(1,"control_req_offset width");
    if ($bits(link.control_req_length)!=64) $fatal(1,"control_req_length width");
    if ($bits(link.control_req_operation)!=16) $fatal(1,"control_req_operation width");
    if ($bits(link.control_req_mode)!=8) $fatal(1,"control_req_mode width");
    if ($bits(link.control_req_use)!=16) $fatal(1,"control_req_use width");
    if ($bits(link.control_req_result_sink_ref)!=64) $fatal(1,"control_req_result_sink_ref width");
    if ($bits(link.control_req_usage_amount)!=64) $fatal(1,"control_req_usage_amount width");
    if ($bits(link.control_rsp_valid)!=1) $fatal(1,"control_rsp_valid width");
    if ($bits(link.control_rsp_ready)!=1) $fatal(1,"control_rsp_ready width");
    if ($bits(link.control_rsp_owner_id)!=32) $fatal(1,"control_rsp_owner_id width");
    if ($bits(link.control_rsp_instance_id)!=16) $fatal(1,"control_rsp_instance_id width");
    if ($bits(link.control_rsp_epoch)!=32) $fatal(1,"control_rsp_epoch width");
    if ($bits(link.control_rsp_task_id)!=32) $fatal(1,"control_rsp_task_id width");
    if ($bits(link.control_rsp_request_seq)!=64) $fatal(1,"control_rsp_request_seq width");
    if ($bits(link.control_rsp_context_id)!=16) $fatal(1,"control_rsp_context_id width");
    if ($bits(link.control_rsp_generation)!=32) $fatal(1,"control_rsp_generation width");
    if ($bits(link.control_rsp_request_id)!=64) $fatal(1,"control_rsp_request_id width");
    if ($bits(link.control_rsp_status)!=16) $fatal(1,"control_rsp_status width");
    if ($bits(link.control_rsp_object_state)!=8) $fatal(1,"control_rsp_object_state width");
    if ($bits(link.control_rsp_cleanup_done)!=1) $fatal(1,"control_rsp_cleanup_done width");
    if ($bits(aix_crypto_secret_w512_req_payload_t)!=736) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w512_rsp_payload_t)!=569) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w512_write_payload_t)!=929) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w512_read_payload_t)!=929) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w512_control_req_payload_t)!=736) $fatal(1,"payload width");
    if ($bits(aix_crypto_secret_w512_control_rsp_payload_t)!=313) $fatal(1,"payload width");
    if (ROLE_KEY_LOAD!=1) $fatal(1,"ROLE_KEY_LOAD encoding");
    if (ROLE_SECRET_MESSAGE_READ!=2) $fatal(1,"ROLE_SECRET_MESSAGE_READ encoding");
    if (ROLE_RESULT_PREPARE!=3) $fatal(1,"ROLE_RESULT_PREPARE encoding");
    if (ROLE_RESULT_WRITE!=4) $fatal(1,"ROLE_RESULT_WRITE encoding");
    if (ROLE_RESULT_COMMIT!=5) $fatal(1,"ROLE_RESULT_COMMIT encoding");
    if (ROLE_RESULT_ABORT!=6) $fatal(1,"ROLE_RESULT_ABORT encoding");
    if (ROLE_REVOKE!=7) $fatal(1,"ROLE_REVOKE encoding");
    if (ROLE_USAGE_RESERVE!=8) $fatal(1,"ROLE_USAGE_RESERVE encoding");
    if (ROLE_QUERY!=9) $fatal(1,"ROLE_QUERY encoding");
    if (ROLE_NONCE_RESERVE!=10) $fatal(1,"ROLE_NONCE_RESERVE encoding");
    if (USE_MAC!=1) $fatal(1,"USE_MAC encoding");
    if (USE_ENCRYPT!=2) $fatal(1,"USE_ENCRYPT encoding");
    if (USE_DECRYPT!=3) $fatal(1,"USE_DECRYPT encoding");
    if (USE_DERIVE!=4) $fatal(1,"USE_DERIVE encoding");
    if (USE_WRAP!=5) $fatal(1,"USE_WRAP encoding");
    if (USE_UNWRAP!=6) $fatal(1,"USE_UNWRAP encoding");
    if (USE_HP!=7) $fatal(1,"USE_HP encoding");
    if (OBJECT_STATE_NONE!=0) $fatal(1,"OBJECT_STATE_NONE encoding");
    if (OBJECT_STATE_PREPARED!=1) $fatal(1,"OBJECT_STATE_PREPARED encoding");
    if (OBJECT_STATE_WRITTEN!=2) $fatal(1,"OBJECT_STATE_WRITTEN encoding");
    if (OBJECT_STATE_ACTIVE!=3) $fatal(1,"OBJECT_STATE_ACTIVE encoding");
    if (OBJECT_STATE_ABORTED!=4) $fatal(1,"OBJECT_STATE_ABORTED encoding");
    if (OBJECT_STATE_UNKNOWN!=5) $fatal(1,"OBJECT_STATE_UNKNOWN encoding");
    wait(reset_n); #1; compare_paths();
    request='1; expected='1; #1; compare_paths();
    for (int sample=1;sample<=128;sample++) begin
      request=2407'(pattern(sample)); expected=1817'(pattern(sample+271));
      #1; compare_paths();
    end
    for (int bitno=0;bitno<2407;bitno++) begin
      request='0; request[bitno]=1'b1; #1; compare_paths(); end
    for (int bitno=0;bitno<1817;bitno++) begin
      expected='0; expected[bitno]=1'b1; #1; compare_paths(); end
    $display("PASS crypto_secret DATA_W=512 fields=103"); done=1;
  end
endmodule
module tb_crypto_secret;
  logic clk=0; logic reset_n=0;
  logic [4:0] done;
  always #5 clk=~clk;
  initial begin #20; reset_n=1; end
  crypto_secret_case_32 case_32(clk,reset_n,done[0]);
  crypto_secret_case_64 case_64(clk,reset_n,done[1]);
  crypto_secret_case_128 case_128(clk,reset_n,done[2]);
  crypto_secret_case_256 case_256(clk,reset_n,done[3]);
  crypto_secret_case_512 case_512(clk,reset_n,done[4]);
  initial begin wait (&done); $display("HWIF_SMOKE_PASS crypto_secret"); $finish; end
  initial begin #100000; $fatal(1,"timeout"); end
endmodule
