CAPI=2:

name: aixsilicon:interface:pwm:0.1.0
description: AIXSILICON PWM interface contract and SystemVerilog views

filesets:
  rtl:
    depend:
      - aixsilicon:interface:common:1.0.0
    files:
      - rtl/pwm_if.sv
    file_type: systemVerilogSource

  contract:
    files:
      - contract/pwm.interface.yaml: {copyto: metadata/pwm.interface.yaml}
    file_type: user

targets:
  default:
    filesets: [rtl]

  contract:
    filesets: [contract]

  lint:
    filesets: [rtl]
