CAPI=2:

name: aixsilicon:interface:memory_1r1w:1.0.0
description: AIXSILICON separate read/write port memory interface contract and views

filesets:
  rtl:
    depend:
      - aixsilicon:interface:common:1.0.0
    files:
      - rtl/memory_1r1w_pkg.sv
      - rtl/memory_1r1w_if.sv
    file_type: systemVerilogSource

  contract:
    files:
      - contract/memory_1r1w.interface.yaml: {copyto: metadata/memory_1r1w.interface.yaml}
    file_type: user

targets:
  default:
    filesets: [rtl]

  contract:
    filesets: [contract]

  lint:
    filesets: [rtl]
