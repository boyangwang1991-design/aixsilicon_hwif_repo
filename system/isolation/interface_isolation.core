CAPI=2:

name: aixsilicon:interface:isolation:0.1.0
description: AIXSILICON isolation interface contract and SystemVerilog views

filesets:
  rtl:
    depend:
      - aixsilicon:interface:common:1.0.0
    files:
      - rtl/isolation_pkg.sv
      - rtl/isolation_if.sv
    file_type: systemVerilogSource

  contract:
    files:
      - contract/isolation.interface.yaml: {copyto: metadata/isolation.interface.yaml}
    file_type: user

targets:
  default:
    filesets: [rtl]

  contract:
    filesets: [contract]

  lint:
    filesets: [rtl]
