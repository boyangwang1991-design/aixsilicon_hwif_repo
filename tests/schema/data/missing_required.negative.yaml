# Negative test: 缺失必填字段（channels）应被拒绝
schema_version: "1.0"

interface:
  id: IFC-BAD-001
  name: aix_bad
  family: test
  semantic_version: 1.0.0
  owner: hw-platform
  lifecycle: draft

roles:
  - id: source

views:
  packed_struct: false
  sv_interface: true
  flattened: false
